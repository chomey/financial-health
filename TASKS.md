# Tasks

- [x] Task 1: [ARCH] Initialize Next.js project with Tailwind CSS and Vitest — Scaffold a Next.js 15 app with TypeScript (strict mode), Tailwind CSS v4, and Vitest. Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --no-import-alias` (use src/ directory). Add Vitest with `npm install -D vitest @vitejs/plugin-react jsdom @testing-library/react @testing-library/jest-dom`. Create `vitest.config.ts` with jsdom environment and React plugin. Configure a custom Tailwind color palette in the CSS theme: warm greens (emerald/teal for positive metrics), warm neutrals (stone/slate for backgrounds), soft blues (for info/links), and a celebratory gold/amber for achievements. Add a `"test": "vitest run"` script to package.json. Verification: `npm run dev` serves a styled page at localhost:3000, `npm test` runs with 0 tests found (no error). [@devops]

- [x] Task 2: Set up Playwright screenshot & test infrastructure — Install Playwright with `npm init playwright@latest` (use tests/e2e/ directory, install browsers). Configure `playwright.config.ts` with baseURL `http://localhost:3000`, webServer that runs `npm run dev`, and a screenshot helper utility at `tests/e2e/helpers.ts` that captures and saves screenshots to `screenshots/` with descriptive filenames. Create `.gitattributes` to track `*.png *.jpg *.jpeg *.gif *.webp *.svg` via Git LFS. Write one smoke test (`tests/e2e/smoke.spec.ts`) that loads `/`, verifies the page renders with the app title, and saves a screenshot. Verification: `npx playwright test` passes, screenshot appears in `screenshots/`. [@qa]

- [x] Task 3: Build app shell & responsive layout — Create the main page layout: a left-hand entry panel and a right-hand dashboard panel, side by side on desktop, stacked on mobile. Add an app header with the name "Financial Health Snapshot" and a warm tagline (e.g., "Your finances at a glance — no judgment, just clarity"). All panels should have subtle hover lift effects, smooth transitions, and a polished empty state. [@frontend]

- [x] Task 4: Build asset entry section with mock data — Create the Assets card in the entry panel. Pre-populate with 2-3 example assets (e.g., "Savings Account: $12,000", "TFSA: $35,000"). Each row shows a category label and dollar amount. Rows highlight on hover, show a delete button on hover. An "Add Asset" button at the bottom smoothly expands a new row with inline editing (click to type category name and amount). Support category suggestions for CA (TFSA, RRSP, RESP, FHSA, LIRA) and US (401k, IRA, Roth IRA, 529, HSA) plus universal ones (Savings, Checking, Brokerage, Home Equity, Vehicle, Other). All values click-to-edit with smooth focus transitions. [@frontend]

- [x] Task 5: Build debt entry section — Create the Debts card, same interaction pattern as assets. Pre-populate with 1-2 example debts (e.g., "Mortgage: $280,000", "Car Loan: $15,000"). Category suggestions: Mortgage, Car Loan, Student Loan, Credit Card, Line of Credit, Personal Loan, Other. Rows have hover highlight, hover-reveal delete, click-to-edit values, smooth add/remove animations. [@frontend]

- [x] Task 6: Build income & expense entry sections — Create two cards: Monthly Income and Monthly Expenses. Income has category suggestions (Salary, Freelance, Investment Income, Side Hustle, Other). Expenses has flexible categories (Rent/Mortgage Payment, Childcare, Groceries, Subscriptions, Transportation, Insurance, Utilities, "Monthly Expenses" as a catch-all, Other). Users can be as granular or broad as they want. Same interaction pattern: hover highlights, click-to-edit, smooth add/remove. Show a monthly total at the bottom of each card that animates when values change. [@frontend]

- [x] Task 7: Build financial goals section — Create a Goals card. Each goal has a name (e.g., "New Car", "Emergency Fund", "Vacation"), a target amount, and a current amount saved. Show an animated progress bar for each goal. Add button expands a new inline goal. Progress bars should have smooth fill animations, color transitions (warm to green as they approach 100%), and a celebratory glow effect when a goal is reached. Hover on a goal shows a tooltip with percentage and remaining amount. [@frontend]

- [x] Task 8: Build snapshot dashboard with mock calculations — Create the right-hand dashboard panel. Display four key metric cards: Net Worth (assets minus debts), Monthly Surplus/Deficit (income minus expenses), Financial Runway (months of expenses covered by liquid assets), and Debt-to-Asset Ratio. Use hardcoded mock values initially. Each metric card should have a large number that counts up on load, a label, a subtle icon, and a hover tooltip explaining what the metric means. Cards lift on hover. Use encouraging color coding (greens for positive metrics). [@frontend]

- [x] Task 9: Build positive insights engine — Below the metric cards, add an Insights section that generates 3-5 human-readable, encouraging messages based on the financial data. Use the actual values from the entry panel (wire the state). Examples: "You could comfortably cover about 18 months of expenses — that's a strong safety net", "Your savings are growing nicely — you're 60% of the way to your New Car goal", "You're spending less than you earn each month — that surplus is building your future". Insights should have a warm card style, subtle entrance animations, and an icon per insight type (shield for runway, trending-up for surplus, target for goals). [@fullstack]

- [x] Task 10: Wire all entry sections to shared state — Connect assets, debts, income, expenses, and goals to a shared React state (useReducer or useState). Dashboard metrics should recalculate live as the user edits any value. Numbers in the dashboard should animate smoothly when they change. Ensure add/edit/delete in any section immediately updates the dashboard. [@fullstack]

- [x] Task 11: Implement URL state persistence — Encode the full app state (all assets, debts, income, expenses, goals) as base85-encoded JSON in a `s=` query param. Update the URL on every state change (using `replaceState`, not `pushState`, to avoid polluting browser history). On page load, read the `s=` param and restore state. Add a "Copy Link" button in the header that copies the current URL to clipboard with a brief "Copied!" animation. Verify that reloading the page preserves all entered data exactly. [@fullstack]

- [x] Task 12: Add region toggle for CA/US financial vehicles — Add a toggle in the header or settings area that lets users choose their region (Canada, United States, or Both). The toggle should use flag icons or short labels (CA/US). When a region is selected, asset and debt category suggestions should filter to show only relevant vehicles (e.g., CA shows TFSA/RRSP but not 401k). "Both" shows all. The selection should persist in the URL state. Smooth transition when toggling. [@frontend]

- [x] Task 13: Add micro-interactions and polish — Audit the entire UI for interactivity polish. Ensure: all buttons have hover, focus, and active states; all cards lift on hover with shadow transitions; number inputs have smooth focus rings; add/remove items have slide-in/slide-out animations; the "runway" metric has a special celebratory glow when it exceeds 12 months; tooltips appear on hover for financial terms (e.g., "Net Worth", "Runway"); empty states have friendly illustrations or messages encouraging the user to add their first item. [@frontend]

- [x] Task 14: Mobile responsiveness pass — Ensure the entire app works beautifully on mobile. Entry panel stacks above dashboard. Cards are full-width. Touch targets are large enough (min 44px). Inline editing works well on mobile keyboards. The "Copy Link" button is easily accessible. Test at 375px, 768px, and 1024px widths. [@frontend]

- [x] Task 15: [MILESTONE] Full E2E test — Write a comprehensive end-to-end test that: loads the app, adds 3 assets, 2 debts, income and expenses, and a goal; verifies dashboard metrics update correctly; copies the URL; reloads the page; verifies all data is preserved; toggles region and verifies category suggestions change. Capture screenshots at each step. [@qa]

- [x] Task 16: Fix setState-during-render bug in all entry components — All 5 entry components (AssetEntry, DebtEntry, IncomeEntry, ExpenseEntry, GoalEntry) call `onChange?.(next)` inside a `setState` updater function, triggering parent state updates during rendering. Fix by moving `onChange` to a `useEffect` that fires when internal state changes. Files: `src/components/AssetEntry.tsx`, `DebtEntry.tsx`, `IncomeEntry.tsx`, `ExpenseEntry.tsx`, `GoalEntry.tsx`. Verification: `npm run dev` — no console warnings about setState during render. [@fullstack]

- [x] Task 17: Make region toggle visibly affect the UI — Toggling CA/US/Both currently has no visible effect unless you're mid-edit on an asset. Fix by: passing `region` prop to DebtEntry and adding CA/US debt categories; showing flag badges (🇨🇦/🇺🇸) next to region-specific categories in dropdowns and saved items; adding a brief visual pulse on entry cards when region changes. Files: `src/app/page.tsx`, `src/components/DebtEntry.tsx`, `src/components/AssetEntry.tsx`. [@frontend]

- [x] Task 18: Replace mortgage mock data with linked Property card — Remove "Mortgage" from debts and "Home Equity" from assets. Add a new Property section with linked value/mortgage/equity: new `Property` interface (`{ id, name, value, mortgage }` with derived equity), new `PropertyEntry.tsx` component with same interaction patterns as other cards. Property values count toward net worth but NOT toward financial runway (illiquid). Default mock: "Home" / $450k value / $280k mortgage / $170k equity. Update URL state encoding, metrics calculations, insights. Files: `src/lib/financial-state.ts`, `src/lib/url-state.ts`, `src/app/page.tsx`, new `src/components/PropertyEntry.tsx`, `src/components/AssetEntry.tsx`, `src/components/DebtEntry.tsx`, `src/lib/insights.ts`. [@fullstack]

- [x] Task 19: Fix z-index and mouseover issues in dashboard column — Tooltips and hover states broken in the right column. Fix z-index stacking, ensure tooltips aren't clipped by overflow, and verify all mouseover interactions work. Files: `src/components/SnapshotDashboard.tsx`, `src/app/page.tsx`. [@frontend]

- [x] Task 20: Fix hydration mismatch in PropertyEntry — Server-rendered property IDs don't match client IDs (e.g., `data-testid="equity-13"` on server vs `data-testid="equity-p1"` on client), causing React hydration errors. The root cause is that mock data IDs are generated with `Date.now()` or `Math.random()` which differ between server and client. Fix by using stable, deterministic IDs for default/mock data (e.g., `"p1"`, `"p2"`). Check all other entry components (AssetEntry, DebtEntry, IncomeEntry, ExpenseEntry, GoalEntry) for the same pattern and fix them too. Verification: `npm run dev` — no hydration mismatch warnings in the browser console. [@fullstack]

- [x] Task 21: Make region toggle obviously useful — The CA/US/Both toggle currently only affects category suggestions when adding new items and shows a subtle 0.6s pulse. Users can't tell what it does. Fix by: (1) adding a visible label or tooltip explaining the toggle's purpose (e.g., "Filter account types by region"); (2) when a region is selected, visually dim or badge existing items that don't belong to that region (e.g., if US is selected, show TFSA rows with a subtle "CA" badge and slightly muted styling); (3) group or sort suggestions by region with section headers in the dropdown; (4) show a brief toast or inline message on toggle like "Showing US account types" so the user gets immediate feedback. The goal: a user toggling this should instantly understand what changed and why. [@frontend]

- [x] Task 22: Add ROI and monthly contributions to assets — Each asset should optionally support an expected annual ROI percentage and a monthly contribution amount. Add two new optional fields to the Asset interface: `roi` (number, annual %) and `monthlyContribution` (number, $). In the UI, show these as secondary fields below each asset row — greyed out placeholder text like "Annual return %" and "Monthly contribution" that become editable on click. When set, display them as subtle badges (e.g., "7% ROI", "+$500/mo") next to the asset value. Pre-populate smart defaults for known account types: 401k/IRA/Roth IRA → 7% ROI, TFSA/RRSP → 5% ROI, Savings → 2% ROI — shown as greyed-out suggestions the user can accept or override. These values feed into the projection graph (Task 25). Update URL state encoding to persist ROI and contribution values. [@fullstack]

- [x] Task 23: Add interest rate and monthly payment to properties — Expand the Property interface with optional fields: `interestRate` (annual %), `monthlyPayment` ($), and `amortizationYears` (number). Display these as secondary detail fields below each property row, similar to Task 22's approach — greyed-out placeholders that become editable on click. When set, show computed info: monthly interest cost vs principal portion, total interest over remaining term, and estimated payoff date. The interest rate should affect the property's impact on growth projections (Task 25) — mortgage interest is a cost that reduces net worth growth. Smart defaults: suggest 5% interest rate, calculate a reasonable monthly payment from the mortgage amount. Update URL state encoding. Files: `src/lib/financial-state.ts`, `src/components/PropertyEntry.tsx`, `src/lib/url-state.ts`. [@fullstack]

- [x] Task 24: Add interest rate to debts — Each debt should optionally support an interest rate (annual %) and minimum monthly payment. Add `interestRate` and `monthlyPayment` optional fields to the debt interface. Show as greyed-out secondary fields below each debt row, editable on click. When set, display as subtle badges (e.g., "19.9% APR", "$150/mo"). Pre-populate smart defaults for known debt types: Credit Card → 19.9%, Car Loan → 6%, Student Loan → 5%, Personal Loan → 8%. These values feed into the projection graph (Task 25) and insights engine (prioritize paying off high-interest debt first). Update URL state and insights. [@fullstack]

- [x] Task 25: Build financial projection timeline graph — Add a time-series projection chart below the dashboard metrics. Install a lightweight chart library (recharts or chart.js). The graph should project net worth over 1–30 years (user-adjustable slider) using data from assets (with ROI + contributions from Task 22), properties (with interest from Task 23), debts (with interest + payments from Task 24), and monthly surplus. Show multiple lines: (1) Total Net Worth projection, (2) one line per goal showing when the goal target is reached (with a marker at the intersection), (3) debt-free date (where all debts hit $0), (4) key milestones like "first $100k", "first $1M". The graph should be interactive — hover to see values at any point in time. Include a toggle for optimistic/conservative/custom scenarios. Make it responsive and visually polished with smooth animations on data changes. [@fullstack]

- [x] Task 26: [MILESTONE] Full E2E test for new features — Comprehensive E2E test covering: asset ROI and contribution fields, property interest rate and payment fields, debt interest rate fields, projection graph rendering with multiple lines, goal target intersections on the graph, timeline slider interaction, region toggle visibility improvements. Capture screenshots at each step. [@qa]

- [x] Task 27: Move projection chart to top of dashboard — The financial projection timeline graph (ProjectionChart) currently renders below the dashboard metrics. Move it to the very top of the dashboard panel so it's the first thing users see. The projection is the most impactful visual — it should be above the metric cards and insights. Reorder the rendering in `SnapshotDashboard.tsx` (or `page.tsx`, wherever the dashboard column is composed). Ensure the chart remains responsive and the rest of the dashboard flows naturally below it. [@frontend]

- [x] Task 28: Add explanatory legend for projection scenario lines — The projection chart shows conservative/moderate/optimistic lines but doesn't explain what they mean. Add a visible legend or info section near the chart that explains: **Conservative** = lower-bound returns (e.g., 2-3% below moderate, accounts for market downturns), **Moderate** = expected returns based on entered ROI values, **Optimistic** = upper-bound returns (e.g., 2-3% above moderate, best-case growth). Show this as a collapsible or always-visible key below the chart with colored line samples matching the chart colors. Also add a brief tooltip on each scenario toggle button explaining the assumption. [@frontend]

- [x] Task 29: Show loan payoff timeline on debts — For each debt that has an interest rate and monthly payment set, calculate and display the estimated payoff date and total interest paid. Show this as an inline summary below the debt row (e.g., "Paid off in 4 years 2 months · $3,200 total interest"). Use standard amortization math: apply monthly interest (`rate/12`) to the balance, subtract the payment, iterate until balance reaches 0. If the monthly payment is less than monthly interest (i.e., the debt will never be paid off), show a warning like "Payment doesn't cover interest — balance will grow". This calculation should be pure client-side in a utility function in `src/lib/`. Update DebtEntry.tsx to display the computed payoff info. [@fullstack]

- [x] Task 30: Support different income frequencies — Currently all income is assumed monthly. Add a frequency field to IncomeItem: monthly (default), weekly, biweekly, quarterly, semi-annually, annually. Show frequency as a subtle dropdown or badge next to each income row (e.g., "Quarterly" badge). Internally normalize all income to monthly equivalent for dashboard calculations (weekly × 52/12, biweekly × 26/12, quarterly ÷ 3, semi-annually ÷ 6, annually ÷ 12). The monthly total at the bottom of IncomeEntry should show the normalized monthly total. Update the IncomeItem interface in `financial-state.ts`, the IncomeEntry component, URL state encoding, and all calculation functions that use income. [@fullstack]

- [x] Task 31: Remove region toggle, add account-type subdivisions to asset/debt dropdowns — Remove the CA/US/Both region toggle entirely (RegionToggle component, region field from state, all region-filtering logic). Instead, restructure the category suggestion dropdowns in AssetEntry and DebtEntry to show grouped subdivisions: a "Canada" section header with CA-specific types (TFSA, RRSP, RESP, FHSA, LIRA), a "USA" section header with US-specific types (401k, IRA, Roth IRA, 529, HSA), and a "General" section with universal types (Savings, Checking, Brokerage, etc.). Users pick what applies to them — no toggle needed. Remove region from FinancialState interface, URL state encoding, and all components that reference it. Clean up RegionToggle.tsx (delete it), remove region-based dimming/badges from entry components, and remove region from mock data. [@fullstack]

- [x] Task 32: Add stock/equity holdings with live price lookup — Add a new "Stocks & Equity" section with a dedicated StockEntry component. Each holding has: ticker symbol (e.g., AAPL, MSFT), number of shares, and current price. When a user types a ticker, fetch the live price from a free API (e.g., Yahoo Finance via a public endpoint, or finnhub.io free tier). Auto-populate the price field and show a "Last updated" timestamp. The total value (shares × price) displays alongside each holding and counts toward total assets and net worth. Include a manual price override for unlisted equity (private stock, RSUs with known value). Add a refresh button to re-fetch all prices. Support optional cost basis field to show gain/loss. Store ticker, shares, and manual price override in URL state (don't persist fetched prices — re-fetch on load). [@fullstack]

- [x] Task 33: Make projection chart full-width above the two-column layout — The projection chart currently sits inside the right dashboard column. Move it out of the two-column layout so it spans the full width of the page, rendered above the entry panel and dashboard columns. Update `page.tsx` to render ProjectionChart in its own full-width section before the two-column grid. Ensure it still receives all the financial data it needs and remains responsive. [@frontend]

- [x] Task 34: Fix mortgage breakdown to show changing interest/principal over time — The property mortgage info currently shows a static "Monthly interest" / "Monthly principal" split based on the current balance, which is misleading — with fixed payments, the interest portion decreases and principal portion increases each month. Fix by: (1) relabeling to "Current month: $X interest / $Y principal" to make clear it's a snapshot; (2) adding a small summary showing how the split changes, e.g., "First year avg: $X interest · Last year avg: $Y interest" or a mini sparkline/bar showing the amortization curve; (3) optionally add a "View schedule" expandable that shows a condensed year-by-year table (year, interest paid that year, principal paid, remaining balance). The `computeMortgageBreakdown` function should stay as-is (it's correct for current month), but add a new `computeAmortizationSchedule` utility that returns yearly summaries. Files: `src/components/PropertyEntry.tsx`, `src/lib/` (new or existing util). [@fullstack]

- [x] Task 35: Prevent double-counting of investment contributions in surplus — Asset monthly contributions (from Task 22) come out of income but aren't reflected in expenses, so surplus is overstated and projections may double-count growth. Fix by: (1) In the Expenses section, auto-generate a read-only "Investment Contributions" summary row that sums all asset monthly contributions — styled distinctly (e.g., italic, with a link icon or "auto" badge) so users know it's derived, not manually entered. (2) Update `computeTotals` to include total monthly contributions in the expense total so surplus = income − expenses − contributions. (3) Ensure the projection engine uses per-asset contributions for asset growth and does NOT also reinvest surplus — surplus should represent truly unallocated money. (4) If a user removes a contribution from an asset, the auto-generated expense row updates immediately. Files: `src/components/ExpenseEntry.tsx`, `src/lib/financial-state.ts`, projection calculation logic. [@fullstack]

- [x] Task 36: [MILESTONE] Full E2E test for tasks 27-35 — Comprehensive E2E test covering: projection chart full-width position at top of page, scenario legend visibility and content, debt payoff timeline display with amortization math verification, income frequency selector and normalized totals, grouped category dropdowns without region toggle, stock entry with ticker input and price display. Capture screenshots at each step. [@qa]

- [x] Task 37: Add country and jurisdiction fields to FinancialState and URL encoding — Extend the `FinancialState` interface with `country?: "CA" | "US"` and `jurisdiction?: string` (province/state code). Add `co` and `ju` fields to `CompactState` in url-state.ts. Update `toCompact` and `fromCompact` to serialize/deserialize these fields. Update `INITIAL_STATE` to default to `country: "CA"`, `jurisdiction: "ON"`. Ensure backward compatibility: if `co`/`ju` are missing from decoded state, default to `"CA"`/`"ON"`. Update `page.tsx` to pass country/jurisdiction through state and into `updateURL`. Write unit tests for round-trip encoding of country/jurisdiction. [@backend]

- [x] Task 38: Add incomeType field to IncomeItem and URL encoding — Extend `IncomeItem` interface in `IncomeEntry.tsx` with `incomeType?: "employment" | "capital-gains" | "other"`. Update `CompactIncome` in url-state.ts with `it?: string` field. Update `toCompact`/`fromCompact` to handle the new field (omit when undefined or "employment" to save URL space). Update `CATEGORY_SUGGESTIONS` to include "Capital Gains" and "Dividends" as categories. Write unit tests for round-trip encoding of income items with incomeType. [@backend]

- [x] Task 39: Build Canadian federal and provincial tax bracket tables — Create `src/lib/tax-tables.ts` with 2025 Canadian federal tax brackets and all 13 provincial/territorial bracket tables. Each jurisdiction is a named export: `CA_FEDERAL_2025`, `CA_ON_2025`, `CA_BC_2025`, etc. Structure: `{ brackets: { min: number; max: number; rate: number }[]; basicPersonalAmount: number }`. Include the capital gains inclusion rate (50% on first $250k, 66.67% above for 2024+). Add a lookup function `getCanadianBrackets(province: string, year?: number)` that returns `{ federal, provincial }`. Include JSDoc comments citing CRA sources. Write unit tests verifying known tax amounts (e.g., $100k ON income should produce approximately $X federal + $Y provincial). [@backend]

- [x] Task 40: Build US federal and state tax bracket tables — Extend `src/lib/tax-tables.ts` with 2025 US federal tax brackets (single filer, as simplest default) and state income tax tables for all 50 states + DC. States with no income tax (TX, FL, WA, NV, WY, SD, AK, NH, TN) get empty bracket arrays. Include standard deduction amounts. For US capital gains, add separate long-term capital gains brackets (0%/15%/20% thresholds). Add `getUSBrackets(state: string, year?: number)` lookup function. Write unit tests verifying known tax amounts for a few states (CA, NY, TX, FL). [@backend]

- [x] Task 41: Build tax computation engine — Create `src/lib/tax-engine.ts` with the core computation function: `computeTax(annualIncome: number, incomeType: "employment" | "capital-gains" | "other", country: "CA" | "US", jurisdiction: string): TaxResult` where `TaxResult = { federalTax: number; provincialStateTax: number; totalTax: number; effectiveRate: number; afterTaxIncome: number; marginalRate: number }`. For employment/other income: apply progressive brackets normally. For capital gains: in Canada, apply 50% inclusion rate then run through brackets; in US, use long-term capital gains rate schedule. Apply basic personal amount / standard deduction. Write comprehensive unit tests covering: employment income in multiple jurisdictions, capital gains in both countries, edge cases (zero income, very high income, no-tax states). [@backend]

- [x] Task 42: Add country and jurisdiction selector UI to page header — Add a country selector (dropdown or segmented control showing flag + "Canada" / "USA") and a dependent province/state selector in the page header area, next to the "Copy Link" button. When country changes, reset jurisdiction to a sensible default (ON for CA, CA for US). Province/state dropdown shows full names with abbreviation codes. Use the same warm styling as the rest of the app. Both selectors persist through the existing URL state mechanism. Style: compact, not dominating the header. Add unit tests for the selector component. [@frontend]

- [x] Task 43: Add income type selector to IncomeEntry rows — Add an income type selector to each income row in `IncomeEntry.tsx`. Show as a small dropdown or pill next to the frequency selector: "Employment" (default, can be hidden/implied), "Capital Gains", "Other". When "Capital Gains" is selected, show a subtle badge or color change on the row to visually distinguish it. The income type selector should also appear in the "add new income" flow. Update category suggestions: when "Capital Gains" is selected as type, suggest categories like "Stock Sale", "Property Sale", "Crypto". Write tests for income type selection and persistence. [@frontend]

- [x] Task 44: Wire tax computation into financial metrics and surplus — Update `computeTotals` in `financial-state.ts` to compute after-tax monthly income using the tax engine. For each income item, annualize (using `normalizeToMonthly * 12`), compute tax based on the item's `incomeType` and the state's `country`/`jurisdiction`, then sum after-tax amounts and convert back to monthly. Add `monthlyAfterTaxIncome` and `totalTaxEstimate` to the totals return value. Update `computeMetrics` to use `monthlyAfterTaxIncome` for surplus calculation. Update surplus breakdown string to show "after-tax income - expenses". Add a new metric card or sub-line showing estimated annual tax and effective rate. Write unit tests comparing pre-tax and after-tax surplus calculations. [@fullstack]

- [x] Task 45: Show tax summary in dashboard and update projections — Add a tax summary display in the dashboard: show estimated annual tax, effective tax rate, and after-tax monthly income as a new metric or as a detail row under Monthly Surplus. Update the projection engine (`projections.ts`) to use after-tax surplus instead of gross surplus for future projections. Update insights engine to potentially generate tax-related insights (e.g., "Your effective tax rate is X% — capital gains income is taxed at a lower rate"). Ensure the projection chart correctly reflects after-tax growth. Write integration tests verifying projections use after-tax values. [@fullstack]

- [x] Task 46: [MILESTONE] Full E2E test for tax computation feature — Comprehensive E2E test covering: country selector switching between CA and US, province/state selector updating based on country, income type selector on income rows, dashboard metrics updating to show after-tax values, surplus calculation reflecting tax deductions, capital gains income showing different effective rates than employment income, URL persistence of country/jurisdiction/incomeType across page reload. Capture screenshots at each step. [@qa]

- [x] Task 47: Add appreciation/depreciation field to properties with dynamic icon — Add an `appreciation` field (annual %) to the Property interface. Allow negative values for depreciating assets like cars (e.g., -15% per year). Update the property icon dynamically: 🏠 for appreciating properties (positive or zero %), 🚗 for depreciating properties (negative %). Show the appreciation rate as a badge next to the property value (e.g., "+3%/yr" in green, "-15%/yr" in red). Wire appreciation into the projection engine so property values grow or shrink over time in the projection chart (currently property values are static). Update URL state encoding with a new `ap` field in CompactProperty. Pre-populate smart defaults: suggest +3% for "Home"/"House"/"Condo" names, -15% for "Car"/"Vehicle" names. Files: `src/components/PropertyEntry.tsx`, `src/lib/financial-state.ts`, `src/lib/projections.ts`, `src/lib/url-state.ts`. [@fullstack]

- [x] Task 48: Build asset allocation pie/doughnut chart — Add a visual allocation chart (pie or doughnut) to the dashboard using recharts. Show how assets break down by category type (retirement accounts, savings, brokerage, stocks, property equity). Use a second ring or toggle to show allocation by a different dimension (e.g., by liquidity: liquid vs illiquid, or by region: CA vs US accounts). The chart should be interactive — hover on a segment to see the category name, dollar value, and percentage. Click a segment to highlight the corresponding entry in the left panel. Use the existing warm color palette. Include a small legend below. Make it responsive (smaller on mobile, full size on desktop). Place it in the dashboard between the metric cards and insights section. [@frontend]

- [x] Task 49: Build expense breakdown visualization — Add a visual breakdown of expenses using a horizontal stacked bar or doughnut chart in the dashboard. Show each expense category as a proportional segment with its name, amount, and percentage of total expenses. Include auto-generated categories (investment contributions, mortgage payments) distinguished with a subtle "auto" indicator. On hover, show the category detail. Sort segments largest-to-smallest for easy scanning. Use warm, distinguishable colors. Add a comparison bar or annotation showing income vs total expenses so users can visually see their surplus gap. Place near the Monthly Surplus metric card or in a dedicated "Spending Breakdown" section. [@frontend]

- [x] Task 50: Build net worth breakdown waterfall chart — Add a waterfall/bridge chart that visually explains how net worth is composed. Start from $0, show each asset category as a positive green block stacking up, then show each debt as a red block pulling down, ending at the net worth total. This makes it immediately clear what's contributing to and detracting from net worth. Use recharts BarChart with stacked/waterfall styling. Each block is hoverable with category name and amount. Include property equity as a distinct block (labeled separately from liquid assets). Show the final net worth bar prominently. Place this as an expandable/collapsible section in the dashboard, or as a tab alongside the allocation chart. [@frontend]

- [x] Task 51: Build "Fast Forward" scenario modeling panel — Add a scenario modeling section below the projection chart. Let users create "what if" scenarios by toggling individual debts on/off (e.g., "What if I paid off my car loan?"), adjusting monthly contribution amounts, changing income, or adding a one-time windfall (bonus, inheritance, property sale). Show the projected impact on net worth, runway, and debt-free date compared to the baseline. Display as a side-by-side comparison: "Current Plan" vs "Scenario" with delta values highlighted (e.g., "Debt-free 2 years sooner", "+$45k net worth at year 10"). Scenarios are temporary (not persisted to URL) — they're exploratory tools. Use the existing projection engine with modified inputs. [@fullstack]

- [x] Task 52: Add benchmark comparisons to dashboard — Add a "How You Compare" section in the dashboard that shows how the user's key metrics stack up against aggregated benchmarks for their age group and country. Use published statistical data: median net worth by age (StatsCan for CA, Federal Reserve SCF for US), recommended savings rates, average debt-to-income ratios, and emergency fund benchmarks (3-6 months). Add an optional age input field (stored in URL state) to personalize benchmarks. Display as horizontal bar comparisons: user's value vs median vs recommended, with encouraging framing (e.g., "Your savings rate is above the median for your age group"). Never show benchmarks in a judgmental way — frame gaps as opportunities, not failures. Include a small info icon explaining data sources. [@fullstack]

- [x] Task 53: Build cash flow Sankey diagram — Add a Sankey/flow diagram that visualizes money flowing from income sources through to expenses, savings, investments, and debt payments. Left side: income sources (salary, freelance, etc.). Middle: after-tax income pool. Right side: branches to each expense category, investment contributions, mortgage/debt payments, and remaining surplus. Line thickness proportional to amount. Interactive — hover on a flow to highlight the path and show the dollar amount. Use a lightweight Sankey library compatible with React (e.g., recharts doesn't support Sankey natively, so evaluate d3-sankey with a thin React wrapper, or @nivo/sankey). Place as a collapsible "Cash Flow" section in the dashboard. This is the most visually distinctive Kubera-inspired feature. [@fullstack]

- [x] Task 54: Add ROI performance tracking to stock holdings — Enhance the StockEntry component to show investment performance metrics. For each stock holding with a cost basis, calculate and display: total return (current value - cost basis), percentage return, and annualized return if a purchase date is added (optional new field). Show gains in green, losses in red. Add a portfolio-level summary at the top of the Stocks section: total portfolio value, total gain/loss, and overall percentage return. In the dashboard, add a "Portfolio Performance" metric card or sub-section showing aggregate stock performance. For assets with ROI fields (from Task 22), show projected vs actual growth comparison where applicable. Store purchase date in URL state. [@fullstack]

- [x] Task 55: [MILESTONE] Full E2E test for Kubera-inspired visualization features — Comprehensive E2E test covering: asset allocation chart rendering with correct segments and hover interaction, expense breakdown chart with category proportions, net worth waterfall chart with positive/negative blocks, Fast Forward scenario panel with debt toggle and comparison display, benchmark section with age input and comparison bars, Sankey diagram rendering with flow paths, stock ROI performance display. Capture screenshots at each step. [@qa]

- [x] Task 56: Build currency utility library and FX rate types — Create `src/lib/currency.ts` with `SupportedCurrency` type (`"CAD" | "USD"`), `FxRates` interface, helper functions (`getHomeCurrency`, `getForeignCurrency`, `fxPairKey`, `convertToHome`, `formatCurrency`, `formatCurrencyCompact`, `getEffectiveFxRates`), and hardcoded fallback rates. Provide currency conversion that handles same-currency no-ops, missing rates with fallback, and manual override support. Write unit tests for all conversion and formatting functions. [@backend]

- [x] Task 57: Build FX rate API endpoint and live rate fetching — Create `src/app/api/fx-rate/route.ts` API endpoint that fetches live CAD/USD rates from open.er-api.com with a 4-hour in-memory cache, input validation, and bidirectional caching. In `page.tsx`, fetch live FX rates on mount and when country changes (skip if manual override is set). Handle errors silently, falling back to hardcoded rates. Write unit tests for the API endpoint. [@backend]

- [x] Task 58: Add per-item currency override to assets, debts, and properties — Add optional `currency` field to Asset, Debt, and Property interfaces. Create `CurrencyBadge.tsx` component that toggles between home and foreign currency on click, showing the converted amount in home currency. Wire into AssetEntry, DebtEntry, and PropertyEntry components. Update `computeTotals` and all calculation functions in `financial-state.ts` to convert per-item amounts to home currency using `convertToHome`. Ensure net worth, surplus, runway, and all metrics reflect multi-currency values. [@fullstack]

- [x] Task 59: Add FX rate display and manual override to header — Create `FxRateDisplay.tsx` component showing the live FX rate with click-to-edit manual override. Display "live" badge for fetched rates or "custom" badge with clear button for manual overrides. Add to page header next to country selector. Persist manual FX override in URL state. [@frontend]

- [x] Task 60: Persist currency fields in URL state encoding — Update `CompactAsset`, `CompactDebt`, `CompactProperty`, and `CompactState` interfaces in `url-state.ts` with `cu` (currency override) and `fxm` (manual FX override) fields. Update `toCompact`/`fromCompact` to serialize/deserialize currency overrides (omit when home currency to save URL space). Update `FinancialState` with `fxRates` (transient) and `fxManualOverride` (persisted) fields. Write unit tests for round-trip encoding of multi-currency state including per-item overrides and FX manual override. [@fullstack]

- [x] Task 61: [MILESTONE] Full E2E test for multi-currency support — Comprehensive E2E test covering: currency badge toggling on assets/debts/properties, FX rate display in header, manual FX override input, converted amounts showing correctly, URL persistence of currency overrides, metrics reflecting multi-currency conversions. Capture screenshots at each step. [@qa]

- [x] Task 62: Build changelog page with version history — Create a `/changelog` page (`src/app/changelog/page.tsx`) that displays a version history of the app. Each version corresponds to a task number (e.g., "v55 — Kubera-inspired visualizations", "v61 — Multi-currency support"). Show entries in reverse chronological order with: version number (task number), title, short description of what changed, and date completed. Pull content from a `src/lib/changelog.ts` data file that exports an array of `{ version: number; title: string; description: string; date: string }` entries. Pre-populate with all completed tasks (1–61) grouped by milestone. Add a link to the changelog in the app header or footer. Style with the same warm design language as the rest of the app. The changelog should be a server component (no interactivity needed). [@frontend]

- [x] Task 63: Classify account types by tax treatment for withdrawal modeling — Add a `taxTreatment` property to the Asset interface: `"tax-free"` (TFSA, Roth IRA, HSA), `"tax-deferred"` (RRSP, 401k, IRA, LIRA, RESP, 529), or `"taxable"` (Savings, Checking, Brokerage, Other). Create a lookup map in `src/lib/withdrawal-tax.ts` that auto-classifies known account categories by their tax treatment. For `"taxable"` accounts, add an optional `costBasisPercent` field (0-100, default 100%) representing the portion of the balance that is original contributions (not gains) — withdrawals of cost basis are tax-free, only gains are taxed. Expose a function `getWithdrawalTaxRate(category: string, country: "CA" | "US", jurisdiction: string, annualWithdrawal: number): { effectiveRate: number; taxFreeAmount: number; taxableAmount: number }` that computes the tax hit on a withdrawal from a given account type. For tax-deferred accounts, the full withdrawal is taxed as income. For taxable accounts, only the gains portion is taxed at capital gains rates. Tax-free accounts have 0% withdrawal tax. Do NOT add UI in this task — this is the data/logic layer only. Write comprehensive unit tests covering all account types in both CA and US jurisdictions. [@backend]

- [x] Task 64: Apply withdrawal tax to financial runway calculation — Update `simulateRunwayWithGrowth` in `financial-state.ts` to account for withdrawal taxes. Currently it withdraws `monthlyWithdrawal` proportionally from all accounts, but $1 withdrawn from an RRSP is worth less than $1 from a TFSA after tax. Change the simulation so that each bucket carries its `taxTreatment` and the withdrawal logic computes the after-tax value of withdrawals. The simulation should: (1) prefer withdrawing from tax-free accounts first (TFSA, Roth IRA), then taxable accounts, then tax-deferred accounts last (to defer the tax hit); (2) gross up withdrawals from taxed accounts so the *after-tax* amount covers the obligation; (3) use the user's jurisdiction to determine the tax rate via the withdrawal-tax module from Task 63. Show the tax-adjusted runway alongside the current runway on the Financial Runway metric card (e.g., "18.5 months → 15.2 months after withdrawal tax"). Update `computeMetrics` to pass account type info through to the simulation. Write unit tests comparing tax-adjusted vs unadjusted runway for mixed portfolios. [@fullstack]

- [x] Task 65: Apply withdrawal tax to projection engine — Update `projectFinances` in `projections.ts` to model withdrawal taxes during the drawdown phase. When monthly obligations exceed income (retirement/drawdown scenario), the projection currently draws from assets without tax consequences. Fix by: (1) tracking each asset bucket's tax treatment; (2) when surplus is negative (drawdown), computing the gross withdrawal needed to cover the after-tax shortfall; (3) applying the smart withdrawal order from Task 64 (tax-free first, taxable second, tax-deferred last); (4) for taxable accounts, only taxing the gains portion based on `costBasisPercent`. Add a new projection line or annotation showing "after-tax withdrawals" so users can see the tax drag on their portfolio. Update the projection chart tooltip to show withdrawal tax impact when hovering over drawdown years. Write unit tests for projection scenarios with mixed account types. [@fullstack]

- [x] Task 66: Add withdrawal tax summary to dashboard and insights — Add a "Withdrawal Tax Impact" section or annotation to the dashboard that shows: (1) estimated tax drag on the runway (how many months shorter it is due to withdrawal taxes); (2) the optimal withdrawal order for the user's specific account mix; (3) a breakdown showing which accounts are tax-free, tax-deferred, and taxable with their balances. Update the insights engine to generate withdrawal-tax-aware insights, e.g., "Your TFSA holds 40% of your savings — that's $X you can withdraw tax-free", "Consider maximizing your TFSA/Roth IRA contributions — withdrawals from your RRSP/401k will be taxed as income", "Your brokerage account has an estimated $X in unrealized gains — withdrawals would trigger capital gains tax". Keep the tone encouraging and educational, never alarming. [@fullstack]

- [x] Task 67: Add capital gains tracking to brokerage/taxable accounts — For assets classified as `"taxable"` (Brokerage, Savings with gains, etc.), add UI support for the `costBasisPercent` field. Show as an optional secondary field: "Cost basis %" with a tooltip explaining "The percentage of this account that represents your original contributions (not investment gains). Gains above this are subject to capital gains tax on withdrawal." Default to 100% (all cost basis, no gains) for new accounts. When set below 100%, show a subtle badge like "~$X unrealized gains" calculated from the balance. The capital gains tax rate used should respect jurisdiction: Canada's 50% inclusion rate for the first $250k, US long-term capital gains brackets (0%/15%/20%). Wire this into the withdrawal tax calculations from Tasks 63-65. Update URL state encoding with the new field. [@fullstack]

- [x] Task 68: [MILESTONE] Full E2E test for withdrawal tax features — Comprehensive E2E test covering: account tax treatment classification (verify TFSA shows as tax-free, RRSP as tax-deferred, Brokerage as taxable), runway with vs without withdrawal tax, projection chart showing tax drag, withdrawal order recommendation, capital gains cost basis field on brokerage accounts, insights mentioning tax-free withdrawal amounts, URL persistence of costBasisPercent. Test both CA and US jurisdictions to verify different tax treatments. Capture screenshots at each step. [@qa]

- [x] Task 69: Build SVG arrow overlay system and source-target element registry — Create `src/components/DataFlowArrows.tsx`: a full-viewport SVG overlay (`position: fixed`, `pointer-events: none`, `z-index: 50`) that renders animated flowing arrows between source and target elements. Build a React context (`DataFlowContext`) with: (1) `registerSource(id: string, ref: RefObject<HTMLElement>, metadata?: { label: string; value: number; color?: string })` for entry sections to register themselves as data sources; (2) `registerTarget(id: string, ref: RefObject<HTMLElement>)` for metric cards to register as arrow targets; (3) `setActiveTarget(id: string | null)` to trigger arrow display on hover; (4) `activeConnections: { sourceId: string; targetId: string; label?: string; value?: number; sign?: "positive" | "negative" }[]` — the current set of arrows to draw. Arrow rendering: use SVG `<path>` with cubic bezier curves that flow naturally between elements (calculate control points based on relative positions — prefer arcing paths that avoid overlapping the page content). Animate with CSS (`stroke-dasharray` + `stroke-dashoffset` animation for a "flowing" effect along the path, 1–2s loop). Arrows should be semi-transparent with a subtle glow (`filter: drop-shadow`). Green arrows for positive contributions, red for negative (debts, expenses). Recalculate positions on scroll and resize via `IntersectionObserver` + `ResizeObserver`. Render nothing when `activeTarget` is null. Write unit tests for the arrow path calculation math and context registration. [@frontend]

- [x] Task 70: Add data-flow source registration to all entry sections — Update `CollapsibleSection` in `page.tsx` (or create a wrapper) to register each section header as a data-flow source using the `DataFlowContext` from Task 69. Register these source IDs: `"section-assets"`, `"section-debts"`, `"section-income"`, `"section-expenses"`, `"section-property"`, `"section-stocks"`. Each registration includes the section's current total value and a display label. When a section is collapsed, the arrow should point to the collapsed header bar (which shows the summary total). When expanded, the arrow should point to the section's total/summary row if visible, falling back to the header. Also register individual high-value items within sections (e.g., each asset row, each debt row) as sub-sources so arrows can fan out to specific items when there are few enough to display clearly (≤5 items). Use `useRef` on each entry row and register via context. Ensure registrations update when values change and unregister on unmount. [@frontend]

- [x] Task 71: Wire Net Worth metric card to show data-flow arrows on hover — Update `MetricCard` in `SnapshotDashboard.tsx` to register as a data-flow target and trigger arrows on mouse enter/focus. For the **Net Worth** card, define these connections: (1) green arrow from `"section-assets"` labeled with the total assets value; (2) green arrow from `"section-stocks"` labeled with total stocks value; (3) green arrow from `"section-property"` labeled with property equity value (only if properties exist); (4) red arrow from `"section-debts"` labeled with total debts value. On hover, all four arrows animate in simultaneously with a staggered entrance (50ms delay between each). Source sections should get a subtle highlight effect (brief border glow matching the arrow color) so users can see exactly which sections feed the number. On mouse leave, arrows fade out (200ms). Also highlight the breakdown text below the metric value to reinforce the formula. Scroll source sections into view if they're off-screen (smooth scroll, don't steal focus). [@fullstack]

- [x] Task 72: Wire Monthly Surplus metric card to show data-flow arrows on hover — For the **Monthly Surplus** card, define connections: (1) green arrow from `"section-income"` labeled with after-tax income value; (2) red arrow from `"section-expenses"` labeled with total expenses; (3) red arrow from `"section-assets"` labeled "contributions" with total monthly contribution amount (if >0); (4) red arrow from `"section-property"` labeled "mortgage" with total mortgage payment (if >0). Same hover behavior as Task 71: staggered entrance, source highlights, fade-out on leave. The surplus formula should feel immediately obvious: income flows in green, everything else flows in red, and the difference is the surplus. [@fullstack]

- [x] Task 73: Wire Estimated Tax, Financial Runway, and Debt-to-Asset Ratio cards — Wire the remaining three metric cards: **Estimated Tax**: green arrow from `"section-income"` showing gross income, with the arrow label showing the effective rate and annual tax amount. Highlight income rows that are capital gains type with a distinct color. **Financial Runway**: green arrow from `"section-assets"` (liquid assets) and `"section-stocks"`, red arrow from `"section-expenses"` (monthly obligations) and `"section-property"` (mortgage payments if any). The arrow labels should make the division clear (numerator vs denominator). **Debt-to-Asset Ratio**: green arrow from all asset sources (assets + stocks + property value), red arrow from `"section-debts"` and `"section-property"` (mortgage portion). Same hover/highlight/fade behavior as Tasks 71-72. [@fullstack]

- [x] Task 74: Wire insights panel items to show data-flow arrows on hover — Each insight in the `InsightsPanel` references specific financial data. On hover/focus of an insight card, show arrows to the relevant source sections. Map insight types to sources: `"runway"` → assets + expenses; `"surplus"` → income + expenses; `"net-worth"` → assets + debts; `"savings-rate"` → income + expenses; `"debt-interest"` → debts (highlight the specific high-interest debt mentioned); `"tax"` → income; `"withdrawal-tax"` → assets (highlight tax-deferred vs tax-free accounts). Insights that mention specific items by name (e.g., "Your Credit Card has a 19.9% interest rate") should arrow to that specific row if it's registered as a sub-source. Use a lighter arrow style (thinner, more transparent) than metric cards to avoid visual overload since insights are more numerous. [@fullstack]

- [x] Task 75: Arrow visual polish — animated flow, responsive handling, and accessibility — Polish the arrow system: (1) Flowing particle animation: add small dots/circles that travel along each arrow path (like data flowing through a pipe), using SVG `<circle>` with `animateMotion` along the path, 2-3 particles per arrow staggered in timing. (2) Responsive behavior: on mobile (< 768px), replace cross-column arrows with a simpler highlight-only mode (source sections get a colored border pulse, no SVG arrows) since arrows across stacked columns would look chaotic. (3) Accessibility: ensure arrows are decorative only (`aria-hidden`), don't interfere with keyboard navigation, and the source highlights are visible to screen readers via `aria-live` region announcing "Net Worth is calculated from: $X assets, $Y stocks, minus $Z debts". (4) Performance: throttle position recalculation to 1 frame per scroll event, use `will-change: transform` on the SVG overlay, and limit to 8 simultaneous arrows max (prioritize by value magnitude if more connections exist). (5) Arrow labels: show a small floating pill at the midpoint of each arrow with the value and sign (+/-), styled to match the arrow color. [@frontend]

- [x] Task 76: [MILESTONE] Full E2E test for data-flow arrow visualization — Comprehensive E2E test covering: hovering Net Worth card shows arrows to assets, stocks, property, and debts sections; hovering Monthly Surplus shows arrows to income and expenses; hovering other metric cards shows correct source connections; hovering an insight shows relevant arrows; arrows disappear on mouse leave; source sections highlight when targeted; arrows render correctly with collapsed sections; mobile viewport shows highlight-only mode without SVG arrows; arrows update correctly when financial data changes (add an asset, verify arrow updates). Capture screenshots of each metric card's arrow display. [@qa]

- [x] Task 77: Replace SVG arrow overlay with spotlight dimming system — Refactor `DataFlowArrows.tsx`: remove `DataFlowArrowOverlay` (SVG arrows, particles, label pills, path geometry functions like `getCenterPoint`, `getEdgePoint`, `calculateArrowPath`, `approximatePathLength`), keep the `DataFlowContext` registration API (`registerSource`, `registerTarget`, `setActiveTarget`, `activeConnections`, `prioritizeConnections`), keep `DataFlowSourceItem`. Add `SpotlightOverlay` component: a fixed div with `rgba(0,0,0,0.7)` backdrop at z-index 40, pointer-events none, 250ms opacity transition — renders only when `activeTarget` is set. Add `FormulaBar` component that reads `activeConnections` from context and renders color-coded computation terms as pills (green for positive sources, red/rose for negative, bold result with equals sign), positioned below the active metric card on desktop and fixed at bottom of viewport on mobile. Add `activeTargetMeta: { label: string; formattedValue: string }` to `DataFlowContext` so FormulaBar knows the result. Update `globals.css`: remove arrow keyframe animations (`arrow-draw`, `arrow-flow`, `arrow-fade-in`, `source-highlight-green/red`, `mobile-border-pulse-*`), replace `[data-dataflow-highlighted]` CSS with spotlight styles (`position: relative; z-index: 45; background: white; transform: scale(1.005); transition: all 250ms ease-out` plus colored borders/shadows for positive/negative). Add `[data-dataflow-active-target]` CSS for the hovered metric card (z-index 45, elevated shadow). Update `MetricCard` in `SnapshotDashboard.tsx`: set `data-dataflow-active-target` attribute and `activeTargetMeta` on hover/focus, remove scroll-into-view behavior. Update `InsightCard` in `InsightsPanel.tsx` with same `data-dataflow-active-target` pattern. Update unit tests: remove SVG geometry tests, add SpotlightOverlay and FormulaBar rendering tests. Files: `src/components/DataFlowArrows.tsx`, `src/app/globals.css`, `src/components/SnapshotDashboard.tsx`, `src/components/InsightsPanel.tsx`, `tests/unit/data-flow-arrows.test.tsx`, `tests/unit/arrow-polish.test.tsx`. [@fullstack]

- [x] Task 78: [MILESTONE] E2E test for spotlight dimming system — Update all data-flow E2E tests to use spotlight assertions instead of SVG arrow assertions. Verify: hovering Net Worth card shows dim overlay (`data-testid="spotlight-overlay"`) plus spotlighted assets/debts/stocks/property sections (with `data-dataflow-highlighted` attribute and opaque backgrounds above overlay) plus formula bar (`data-testid="formula-bar"`) with correct color-coded computation terms; hovering Monthly Surplus shows income spotlighted green and expenses spotlighted red with formula bar showing after-tax income minus expenses minus contributions minus mortgage; formula bar displays correct terms for Estimated Tax, Financial Runway, and Debt-to-Asset Ratio cards; spotlight clears on mouse leave with no residual highlights; mobile viewport (375px) shows formula bar fixed at bottom of viewport; keyboard Tab to metric card triggers spotlight on focus; no Cumulative Layout Shift detected during spotlight activation/deactivation; insight card hover shows spotlight with lighter styling. Update test files: `tests/e2e/data-flow-arrows.spec.ts`, `tests/e2e/net-worth-data-flow.spec.ts`, `tests/e2e/monthly-surplus-data-flow.spec.ts`, `tests/e2e/remaining-metric-data-flow.spec.ts`, `tests/e2e/insight-data-flow.spec.ts`, `tests/e2e/arrow-polish.spec.ts`. Capture screenshots of each metric card's spotlight display. [@qa]

- [x] Task 79: Replace spotlight dimming with click-to-explain whiteboard mode — Complete overhaul of the data-flow visualization. Remove the hover-triggered spotlight/dimming system entirely (SpotlightOverlay, FormulaBar, full-page rgba overlay, `data-dataflow-active-target` CSS, `data-dataflow-highlighted` CSS). Replace with a **click-triggered whiteboard explainer modal**. When a user clicks a metric card (Net Worth, Monthly Surplus, etc.), open a full-screen or near-full-screen modal (`z-index: 50`, white background, `data-testid="explainer-modal"`) that shows ONLY the relevant source sections cloned/extracted into the modal, laid out cleanly with generous spacing. The modal should feel like a teacher's whiteboard explanation: (1) Show the metric title and value prominently at the top. (2) Below it, render the relevant source cards (e.g., for Net Worth: the Assets section, Stocks section, Property section, Debts section) as read-only summary cards showing category name and total value — not the full editable entry components, just clean summary boxes. (3) Draw hand-drawn-style SVG annotations connecting the source boxes to the result: rough oval/circle annotations around each source total (using a wobbly SVG path that looks hand-drawn — randomize control points slightly for organic feel), with thick marker-style lines (stroke-width 3-4px, rounded linecap, slightly transparent). Green ovals for positive contributions, red for negative. (4) Show the arithmetic clearly: large "+" and "−" operator symbols between source boxes, with a horizontal rule and the "= $X" result in bold at the bottom, like a whiteboard equation. (5) Click anywhere outside the modal or press Escape or click an "X" button to close. (6) The modal should animate in (scale from 0.95 to 1.0 + fade, 200ms) and out (reverse). Keep the `DataFlowContext` registration API and connection definitions in `page.tsx` — the modal reads `activeConnections` and `activeTargetMeta` from context. Change MetricCard in `SnapshotDashboard.tsx` from `onMouseEnter`/`onMouseLeave` to `onClick` that sets the active target. Change InsightCard in `InsightsPanel.tsx` the same way. Remove all hover-based activation. Add a subtle "click to explain" hint on metric cards (small info icon or cursor change to pointer). For the hand-drawn SVG style: create a utility function `handDrawnOval(cx, cy, rx, ry): string` that returns an SVG path with slightly randomized control points (jitter of 2-5px) to create a wobbly oval effect, and `handDrawnLine(x1, y1, x2, y2): string` for connector lines with slight curve/wobble. Use a marker-like color palette: green (`#059669` at 60% opacity), red (`#e11d48` at 60% opacity), with `stroke-linecap: round`. Files: `src/components/DataFlowArrows.tsx` (remove SpotlightOverlay/FormulaBar, add ExplainerModal), `src/components/SnapshotDashboard.tsx` (MetricCard click handler), `src/components/InsightsPanel.tsx` (InsightCard click handler), `src/app/globals.css` (remove spotlight CSS, add modal CSS), `src/app/page.tsx` (pass section summary data to context). Write unit tests for hand-drawn SVG path generation and modal rendering. [@fullstack]

- [x] Task 80: Build source summary cards for explainer modal — Create a `SourceSummaryCard` component used inside the ExplainerModal from Task 79. For each source section referenced by the active metric, render a clean read-only summary card that shows: (1) the section icon and title (e.g., "Assets"), (2) a list of individual items with their values (e.g., "Savings Account: $5,000", "TFSA: $22,000", "RRSP: $28,000"), (3) a bold total at the bottom with a hand-drawn oval annotation around it. The card should have a colored left border (green for positive, red for negative) and white background with subtle shadow. For sections with many items (>6), show the top 5 by value and a "+N more" summary row. The individual item data comes from the `DataFlowContext` — extend source registration to include an `items` array: `{ label: string; value: number }[]` alongside the existing `label` and `value` metadata. Update `CollapsibleSection` in `page.tsx` to pass item-level data when registering (e.g., for Assets, pass each asset's category and amount). Update `DataFlowSourceItem` registrations in entry components to contribute to the parent source's items array. Write unit tests for SourceSummaryCard rendering with varying item counts. [@fullstack]

- [x] Task 81: Add whiteboard-style SVG annotations and arithmetic layout to explainer modal — Enhance the ExplainerModal with the full whiteboard aesthetic: (1) **Hand-drawn ovals**: wrap each SourceSummaryCard's total value in a wobbly SVG oval annotation (using the `handDrawnOval` utility from Task 79). The oval should appear to be drawn on — use a `stroke-dasharray` animation that draws the oval over 400ms on modal open, like a marker being dragged across a whiteboard. (2) **Connector lines**: draw wobbly hand-drawn lines from each source card's oval to a central equation area. Lines should have arrowheads (simple triangular SVG markers). Green lines for positive sources, red for negative. (3) **Arithmetic layout**: between the source cards, show large "+" and "−" symbols (24-32px, bold, colored green/red). Below all sources, render a horizontal hand-drawn line (wobbly) as a "sum bar", then the result: "= $202,000" in large bold text with the metric name. (4) **Sequenced animation**: on modal open, animate in this order: source cards fade in (0-200ms), ovals draw themselves (200-600ms), connector lines draw (400-800ms), operator symbols fade in (600-800ms), sum bar draws (800-1000ms), result value counts up (1000-1200ms). Each step uses CSS animations or `requestAnimationFrame`. (5) **Hand-drawn style tuning**: the wobbly paths should look organic but not sloppy — use Perlin-like noise or simple sinusoidal jitter (2-3px amplitude, ~5 bumps per oval). Stroke should use `stroke-linecap: round`, `stroke-linejoin: round`, and a slightly rough opacity (0.7). Write unit tests for animation sequencing and SVG path wobble within expected bounds. [@frontend]

- [x] Task 82: [MILESTONE] E2E test for whiteboard explainer modal — Comprehensive E2E test covering: clicking Net Worth metric card opens explainer modal with Assets, Stocks, Property, and Debts summary cards showing correct individual items and totals; modal shows hand-drawn oval annotations around totals; arithmetic layout displays correct operators and result; clicking Monthly Surplus shows income and expense breakdowns; clicking Estimated Tax shows income with effective rate; clicking Financial Runway shows liquid assets vs obligations; clicking Debt-to-Asset shows all assets vs all debts; clicking an insight card opens the modal for its relevant sources; modal closes on Escape key, X button, and clicking outside; modal has entrance/exit animations; on mobile (375px) the modal is full-screen with scrollable content; keyboard navigation works (Tab through source cards, Escape to close); metric cards show "click to explain" cursor/hint; hand-drawn SVG paths render (verify SVG path elements exist with non-zero d attributes). Capture screenshots of each metric's explainer modal. [@qa]

- [x] Task 83: Refine hand-drawn SVG circles to use larger sweeping arcs — The current `handDrawnOval` utility produces too much jitter, making circles look messy/squiggly. Replace the implementation with larger, smoother sweeping arcs: reduce the number of control point perturbations from ~5 bumps per oval to 2-3 gentle undulations, increase the wavelength of the sinusoidal jitter so each "wobble" spans 30-40% of the oval circumference instead of 10-15%, reduce jitter amplitude from 2-5px to 1-2px for small ovals and 2-3px for large ovals (scale jitter with oval size), and use a single smooth cubic bezier per quadrant (4 curves total) instead of many small segments. The result should look like a confident teacher circling something with a single fluid pen stroke — one smooth sweep, not a shaky hand. Similarly update `handDrawnLine` to use gentler curves with just 1-2 slight bends over the full line length. Update unit tests to verify the new paths have fewer control points and smoother geometry (e.g., assert max deviation from perfect oval is within bounds). Files: `src/components/DataFlowArrows.tsx` (or wherever the SVG utils live). [@frontend]

- [x] Task 84: Build Estimated Tax explainer with bracket visualization — When the Estimated Tax metric card is clicked, the ExplainerModal should show a tax-specific breakdown instead of the generic source-card layout. Display: (1) a horizontal stacked bar showing the user's income divided into tax brackets, with each bracket segment colored by rate (lighter green for low brackets, deeper green/teal for higher brackets), labeled with the bracket rate percentage. (2) Below the bar, show two summary rows: "Federal: $X" and "Provincial/State: $Y" with the jurisdiction name. (3) Show effective rate prominently: "Effective rate: X.X%" with a visual comparison to the marginal rate. (4) If the user has capital gains income, show a separate section explaining the inclusion rate (CA: 50% of first $250k, 66.67% above) or long-term cap gains brackets (US: 0%/15%/20%). (5) Show after-tax income at the bottom: "Gross $X → Tax $Y → After-tax $Z". Use the existing `computeTax` function from `src/lib/tax-engine.ts` and bracket tables from `src/lib/tax-tables.ts` — call `getCanadianBrackets` or `getUSBrackets` to get the bracket data for rendering. Add a `metricType` field to `activeTargetMeta` in the DataFlowContext so the ExplainerModal can render metric-specific content. Files: `src/components/DataFlowArrows.tsx` (ExplainerModal), `src/lib/financial-state.ts` (pass tax details to MetricData), `src/components/SnapshotDashboard.tsx` (pass metricType on click). [@fullstack]

- [x] Task 85: Build Financial Runway explainer with interactive burndown chart — When the Financial Runway metric card is clicked, the ExplainerModal should show an interactive burndown chart instead of the generic source-card layout. Display: (1) A recharts AreaChart showing total liquid balance (y-axis, $) over months (x-axis) as accounts are drawn down. Generate the data by running a month-by-month simulation (reuse the logic from `simulateRunwayWithGrowth` / `simulateRunwayWithTax` in `financial-state.ts` but capture the full time series, not just the final month count). Show each account as a stacked area (different colors per account category). (2) **Interest/ROI modeling**: overlay a subtle annotation or second line showing what the balance would be WITHOUT investment growth (straight-line drawdown) so users can see the impact of ROI. Label the gap as "Investment growth extended runway by X months". (3) **Withdrawal tax impact**: instead of a separate "after withdrawal tax" sub-line on the metric card, show it directly on the burndown chart as a second line/area — the tax-adjusted drawdown runs out sooner. Shade the gap between the two lines and label it "Tax drag: X months". Remove the `runwayAfterTax` sub-line from the Financial Runway MetricCard since it's now shown in the chart. (4) Below the chart, show the withdrawal order used: "1. TFSA (tax-free) → 2. Brokerage (capital gains) → 3. RRSP (taxed as income)" with each account's starting balance and estimated tax cost. (5) Monthly obligation breakdown: "$X expenses + $Y mortgage = $Z/month". Create a new function `simulateRunwayTimeSeries(buckets, monthlyWithdrawal, ...)` in `financial-state.ts` that returns `{ month: number; balances: Record<string, number>; totalBalance: number }[]` for charting. Files: `src/components/DataFlowArrows.tsx` (ExplainerModal), `src/lib/financial-state.ts` (new time series function), `src/components/SnapshotDashboard.tsx` (remove runwayAfterTax display). [@fullstack]

- [x] Task 86: Include asset ROI in Monthly Surplus calculation and explainer — Investment returns are real wealth creation and especially critical for retirees with no employment income — without ROI, their surplus is just negative expenses, which is useless. Update `computeMetrics` in `financial-state.ts` to include estimated monthly investment returns in the surplus calculation: for each asset with a non-zero ROI, add `balance * (roi/100) / 12` to the income side of the surplus formula. Create a helper function `computeMonthlyInvestmentReturns(assets): { label: string; amount: number; balance: number; roi: number }[]` that returns per-asset monthly return breakdowns. Update the surplus formula: `monthlyAfterTaxIncome + totalMonthlyInvestmentReturns - monthlyExpenses - totalMonthlyContributions - totalMortgagePayments`. In the Monthly Surplus explainer modal: (1) show regular income sources as before (Salary, etc.) in the income summary card, (2) add a separate "Investment Returns" sub-section below regular income showing each asset with its estimated monthly return: e.g., "RRSP ($28k @ 5%) → +$117/mo", "TFSA ($22k @ 5%) → +$92/mo", with a distinct visual style (green dashed border) to distinguish paper returns from cash income, (3) show total investment returns as a summary row. Update the `dataFlowConnections` for Monthly Surplus in `page.tsx` to include an investment returns connection. Update existing surplus-related unit tests to account for the new ROI component. Files: `src/lib/financial-state.ts`, `src/components/DataFlowArrows.tsx` (ExplainerModal, Monthly Surplus variant), `src/app/page.tsx` (connection definitions), `tests/unit/financial-state.test.ts`. [@fullstack]

- [x] Task 87: Scrollable source summary cards with frozen total pane — Replace the "+N more" truncation in `SourceSummaryCard` with a scrollable item list and a sticky/frozen total row at the bottom. Currently, sections with >5 items show the top 5 and a "+N more" label, hiding useful detail. Changes: (1) Remove the `MAX_VISIBLE_ITEMS` constant and the truncation logic — show ALL items always. (2) Wrap the item list `<ul>` in a scrollable container with `max-h-[200px] overflow-y-auto` so long lists scroll vertically within the card. (3) Make the total row sticky at the bottom of the card using `sticky bottom-0 bg-white` so it stays visible while scrolling through items — this acts as a frozen pane. Add a subtle top border shadow (`shadow-[0_-2px_4px_rgba(0,0,0,0.05)]`) on the sticky total to visually separate it from scrolling content. (4) Increase card size: bump padding from `p-4` to `p-5`, increase the total font from `text-lg` to `text-xl`, and widen the modal from `max-w-lg` to `max-w-xl` to give cards more breathing room. (5) Add a subtle scrollbar style (thin, stone-colored) using Tailwind's `scrollbar-thin` or custom CSS. Update unit tests in `tests/unit/source-summary-card.test.tsx`: remove the "+N more" truncation tests, add tests verifying all items render regardless of count, verify the total row has sticky positioning. Files: `src/components/DataFlowArrows.tsx` (SourceSummaryCard, ExplainerModal max-width), `tests/unit/source-summary-card.test.tsx`. [@frontend]

- [x] Task 88: Withdrawal Tax Impact card — auto-expand details and add suggested disclaimer — The `WithdrawalTaxSummary` component currently hides account breakdowns and withdrawal order behind a "Show details" toggle. Remove the toggle and show details expanded by default — they're concise enough to always display. Also rename "Optimal withdrawal order" to "Suggested withdrawal order" and add a small disclaimer below it: "We don't have full visibility into each account's tax implications — this is a rough suggestion. Consult a tax professional for personalized advice." Keep the disclaimer subtle (text-xs text-stone-400 italic). Files: `src/components/WithdrawalTaxSummary.tsx`, update any related tests. [@frontend]

- [x] Task 89: Estimated Tax card click-to-explain when tax is $0 — Currently clicking the Estimated Tax metric card does nothing when tax is CA$0 (no income entered), because the connection filter removes entries with value=0. The explainer should still open and show the jurisdiction's tax system — e.g., "You're in Ontario, Canada. Here's how income tax works here:" with the bracket table for informational purposes even when there's no income. Changes: (1) In the `handleClick` filter in MetricCard, allow the Estimated Tax connections through even when value is 0. (2) In `buildTaxExplainerDetails`, return a details object even when `grossAnnualIncome === 0` — populate the brackets from the jurisdiction tables with zero amounts so the bracket bar shows the rate structure (empty bar but with legend), and show effective/marginal rates as 0%. (3) The TaxExplainerContent should render a friendly message when income is 0: "No income entered — add income to see your estimated tax breakdown. Here are the {jurisdiction} tax brackets for reference:" followed by the bracket legend. Files: `src/lib/financial-state.ts` (buildTaxExplainerDetails), `src/components/SnapshotDashboard.tsx` (handleClick filter), `src/components/DataFlowArrows.tsx` (TaxExplainerContent zero state). [@fullstack]

- [x] Task 90: Move runway burndown chart from explainer modal to main page — The runway burndown chart (stacked area chart showing account drawdown over time) is currently only visible inside the Financial Runway explainer modal, where it's too narrow in the sidebar to read properly. Move it to the main page as a full-width section placed directly below the Projection Chart / InsightsPanel in the `<section id="projections">` area. Create a new component `RunwayBurndownChart` that renders the recharts AreaChart currently in `RunwayExplainerContent`, but sized for full-width display. The chart should still show: stacked account areas by category, "without growth" dashed comparison line, "after tax" line with shaded tax drag area, growth extension and tax drag month annotations, and withdrawal order below the chart. Compute the `RunwayExplainerDetails` data in `page.tsx` (using `simulateRunwayTimeSeries` from financial-state.ts) and pass it as a prop. Wrap in `ZoomableCard`. In the Financial Runway explainer modal, replace the full chart with a condensed summary (monthly obligations + withdrawal order) and a note "See the burndown chart above for the full visualization." Files: `src/components/RunwayBurndownChart.tsx` (new), `src/app/page.tsx` (add to projections section), `src/components/DataFlowArrows.tsx` (simplify RunwayExplainerContent), `src/lib/financial-state.ts` (export needed helpers). [@fullstack]

- [ ] Task 91: Asset ROI tax treatment toggle — income vs capital gains — Some asset accounts (high-interest savings, GICs, money market funds) generate returns taxed as regular income, not capital gains. Add a `roiTaxTreatment` field to the `Asset` type with values `"capital-gains"` (default) or `"income"`. In the AssetEntry UI, add a small toggle or dropdown next to the ROI field that lets users pick "Capital gains" or "Interest income" — show it only when ROI > 0. Default: savings-type categories (Savings Account, GIC, Money Market, HISA) default to `"income"`, investment categories default to `"capital-gains"`. Tax-sheltered accounts (TFSA, Roth IRA, FHSA) should hide this toggle since ROI is tax-free regardless. Wire the new field into the withdrawal tax simulation: in `simulateRunwayWithTax` and `computeWithdrawalTaxSummary`, pass `roiTaxTreatment` so income-taxed accounts use the marginal income tax rate instead of capital gains rate when computing withdrawal tax gross-up. Update `getTaxTreatment` in `src/lib/withdrawal-tax.ts` or add a parallel helper that considers ROI tax treatment. Persist the field in URL state. Files: `src/components/AssetEntry.tsx` (UI toggle, type update), `src/lib/financial-state.ts` (pass to tax sim), `src/lib/withdrawal-tax.ts` (use in tax calc), `src/lib/url-state.ts` (persist). [@fullstack]

- [ ] Task 92: Simplify runway burndown chart for clarity — The current burndown chart is confusing: too many overlapping stacked areas (one per account category), plus dashed lines and shaded regions all competing for attention. Redesign for clarity: (1) **Replace stacked category areas with 2-3 simple lines**: a solid green line for "With investment growth" (total balance), a dashed gray line for "Without growth" (simple drawdown), and an amber line for "After withdrawal taxes" (only if tax drag > 0). No per-account stacking — users care about the total, not which account drains first. (2) **Add milestone markers**: vertical dotted lines where each scenario's balance hits $0, labeled with the month count (e.g., "22 mo" / "1.8 yr"). Add a horizontal dashed line at the 6-month emergency fund threshold for context. (3) **Add a plain-English summary sentence** above the chart: "Your savings could last ~22 months. Investment growth adds ~5 months, but withdrawal taxes reduce it by ~3." Generated dynamically from the runway data. (4) **Replace the stacked area legend** with a clean, readable legend showing just the 2-3 line types with their colors. (5) Keep the withdrawal order pills and monthly obligation breakdown below the chart as-is. (6) Below the chart, show a compact starting balances row: "Starting: TFSA $X · RRSP $Y · Savings $Z" so users still see the per-account detail without cluttering the chart. Files: `src/components/RunwayBurndownChart.tsx`, update related tests. [@frontend]

- [ ] Task 93: [MILESTONE] E2E test for explainer and tax treatment enhancements — Comprehensive E2E test covering: (1) Withdrawal Tax Impact card shows details auto-expanded (no "Show details" toggle), withdrawal order labeled "Suggested" with disclaimer text visible. (2) Clicking Estimated Tax card with $0 income still opens explainer showing jurisdiction bracket info. (3) Runway burndown chart renders on main page below projections (full-width, not in modal). (4) Clicking Estimated Tax card with income opens bracket bar, federal/provincial breakdown, effective vs marginal rates. (5) Asset ROI tax treatment toggle visible on savings/GIC accounts, hidden on TFSA/Roth, defaults correct. (6) Source summary cards show all items with scrollable list and frozen total. (7) All modals close correctly on Escape/click-outside. Capture screenshots. [@qa]
