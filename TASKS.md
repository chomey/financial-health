# Tasks

<!-- Older tasks archived to TASKS-ARCHIVE.md -->

<!-- Completed tasks archived to TASKS-ARCHIVE.md. Last completed: Task 124. -->
<!-- Ralph picks up the first unchecked task and works on exactly one per iteration. -->

- [x] Task 127: [OPUS] Visual theme overhaul — soft cyberpunk palette — Replace the current green/stone Tailwind theme with a soft cyberpunk aesthetic: dark-ish cool background (slate-900/950), muted neon accents (cyan, violet, rose) instead of bright greens, semi-transparent glass-effect cards with subtle borders. The vibe is "easy on the eyes at night" — no blindingly bright colors, no harsh contrasts. Update `globals.css` `:root` and `@theme inline` color tokens. Update `--background` and `--foreground` for the dark base. Add CSS custom properties for accent colors (e.g. `--accent-positive`, `--accent-negative`, `--accent-info`). This task ONLY changes the theme tokens and globals.css — component updates are in subsequent tasks. Unit test that the CSS file contains the new token names. [@frontend]

- [x] Task 128: Update metric cards and dashboard for new theme — Apply cyberpunk palette to `SnapshotDashboard.tsx` metric cards: dark card backgrounds with glass effect (bg-white/5 backdrop-blur border-white/10), **green (emerald-400/500) for positive values, red (red-400/500) for negative values** — classic red/green financial color coding, not muted pastels. Stone-300/400 for labels. Stronger glow-pulse and warning-pulse keyframes in globals.css using green/red accents. Update `DataFlowArrows.tsx` explainer modal: dark background, fix the tax bracket bars — use dark-on-light text (never white text on light green). Each bracket bar should use a distinct gradient. E2E screenshot test verifying readability. Files: `src/components/SnapshotDashboard.tsx`, `src/components/DataFlowArrows.tsx`, `src/app/globals.css`. [@frontend]

- [x] Task 129: Update entry panels and input sections for new theme — Apply cyberpunk palette to all entry components: `AssetEntry.tsx`, `DebtEntry.tsx`, `IncomeEntry.tsx`, `ExpenseEntry.tsx`, `PropertyEntry.tsx`, `GoalEntry.tsx`. Dark card surfaces (slate-800/900), subtle glowing borders (white/10), input fields with dark backgrounds and light text. Hover/focus states use **vivid accent glows** — not faded pastels. "Add" buttons use bright accent colors. Section totals use **green for positive, red for negative**. Auto-computed rows (tax, investment returns) should have a distinct badge style with a subtle glow. E2E screenshot test. [@frontend]

- [x] Task 130: Update charts and projection panel for new theme — Apply cyberpunk palette to all chart components: `NetWorthDonutChart.tsx`, `ProjectionChart.tsx`, `ExpenseBreakdownChart.tsx`, `WaterfallChart.tsx`. Chart backgrounds transparent/dark, grid lines subtle (white/5), axis labels in light text. Update Recharts color arrays to use **vivid, saturated colors** (cyan, violet, amber, rose, emerald) — not washed out. CRITICAL: every line/series on every chart must use a visually distinct color — no two series should share the same hue. Projection chart lines should glow. **Positive bars/areas green, negative bars/areas red.** Summary tables use dark rows with good contrast. Legend text in light colors. E2E screenshot test. [@frontend]

- [ ] Task 131: Update page layout, header, and remaining UI for new theme — Apply cyberpunk palette to `page.tsx` layout: dark page background, header with subtle gradient or glass effect, country toggle buttons with accent borders, insights panel with dark cards and vivid accent highlights. Update `FastForwardPanel.tsx` sliders and controls with bright accent colors. Update print styles in globals.css to override back to white background for printing. Final pass on any remaining hardcoded colors across all components (grep for `bg-white`, `text-stone-`, `border-stone-` etc and update). Ensure **red/green positive/negative convention is consistent everywhere**. E2E screenshot of full page. [@frontend]

- [ ] Task 132: [OPUS] Show company/fund names for stock tickers — Create `src/lib/ticker-names.ts` with a static `Record<string, string>` map of ticker symbols to full company/fund names. Include ALL S&P 500 companies, plus popular ETFs (VOO, VTI, VGT, VGK, VFIAX, QQQ, SPY, IWM, etc.) and popular Canadian tickers (XIU, XIC, VFV, etc.) — at minimum 550+ entries. Display the full name as a subtle secondary line below the ticker in both the Stocks & Equity detail view (`AssetEntry.tsx` stock rows) and the explainer modal source cards (`DataFlowArrows.tsx`). For tickers NOT in the static map, make a client-side web search/API call to resolve the name (e.g. a free finance API or search endpoint), then cache the result in a React ref or module-level Map so it's only fetched once per session. Show a brief loading state while fetching. If the lookup fails, show nothing (graceful fallback). Export a `getTickerName(ticker: string): string | undefined` sync function for the static map and a `resolveTickerName(ticker: string): Promise<string | undefined>` async function for the fallback. Unit tests for static lookup and cache behavior. E2E test that names appear for known tickers. Files: `src/lib/ticker-names.ts` (new), `src/components/AssetEntry.tsx`, `src/components/DataFlowArrows.tsx`. [@fullstack]

- [ ] Task 133: Make Income Replacement metric card clickable with detailed explainer — When the user clicks the Income Replacement metric card, open an explainer modal (same pattern as the existing DataFlowArrows explainer) showing: (1) the formula breakdown: total invested assets, the 4% SWR amount, monthly income, and how the percentage is derived; (2) a per-asset breakdown showing each invested asset's contribution to the 4% withdrawal (e.g. "Brokerage $500k -> $1,667/mo", "401k $200k -> $667/mo"); (3) the current tier label and what's needed to reach the next tier (e.g. "You need $X more invested to reach 'Building momentum'"); (4) a brief explanation of the 4% rule and why it matters. Wire up DataFlowConnectionDefs from the asset sources to the Income Replacement metric card. Unit tests for the breakdown computation. E2E test that clicking opens the explainer with correct values. Files: `src/components/SnapshotDashboard.tsx`, `src/components/DataFlowArrows.tsx`, `src/lib/financial-state.ts`, `src/app/page.tsx`. [@fullstack]

- [ ] Task 134: [MILESTONE] Visual theme E2E regression — Full Playwright regression test verifying the cyberpunk theme renders correctly across all major views: (1) main dashboard with metric cards, (2) entry panels with inputs, (3) explainer modal with tax brackets readable, (4) projection chart with new colors, (5) mobile responsive view. Capture screenshots. Verify no accessibility contrast ratio regressions (text must meet WCAG AA 4.5:1 for normal text). [@qa]

- [ ] Task 135: Add debt-to-income ratio insight with educational explainer — Calculate debt-to-income ratio (total monthly debt payments ÷ gross monthly income) and add a new insight type `"debt-to-income"` to `src/lib/insights.ts`. Tiers: <20% = "Excellent — lenders see you as low risk", 20-35% = "Good — you qualify for most loans", 36-43% = "Moderate — this is the upper limit for most mortgage approvals", 44%+ = "High — lenders may decline new credit". The insight message should **explain why this matters**: lenders use DTI to decide mortgage approvals, credit card limits, and loan rates — a lower DTI means better terms and more financial flexibility. Show the actual ratio and what tier the user falls in. Add `monthlyDebtPayments` to `FinancialData` interface and compute it in `financial-state.ts` from debt minimum payments + mortgage payments. Unit tests for each tier. E2E test. Files: `src/lib/insights.ts`, `src/lib/financial-state.ts`, `src/app/page.tsx`. [@fullstack]

- [ ] Task 136: Add housing cost ratio insight with 30% rule explanation — Calculate housing cost ratio (monthly rent or mortgage payment ÷ gross monthly income) and add insight type `"housing-cost"` to `src/lib/insights.ts`. Tiers: <25% = "Well within budget — leaves plenty of room for savings", 25-30% = "Right at the sweet spot — the classic 30% guideline", 31-40% = "Above the 30% rule — common in expensive cities but limits savings capacity", 40%+ = "Cost-burdened — HUD defines this as housing cost-burdened, consider ways to reduce". Explain **why 30% matters**: it's the guideline used by financial planners and HUD — spending more leaves less for emergencies, retirement savings, and life goals. Flag when over 30% with a gentle nudge. Compute from mortgage payments in `PropertyEntry` or a rent expense category. Add `monthlyHousingCost` to `FinancialData`. Unit tests for each tier. E2E test. Files: `src/lib/insights.ts`, `src/lib/financial-state.ts`, `src/app/page.tsx`. [@fullstack]

- [ ] Task 137: [OPUS] Implement Coast FIRE age calculation and insight — Coast FIRE is the age at which your current investments, growing at an assumed real return (e.g. 7% nominal - 3% inflation = ~5% real), will compound to cover your retirement expenses by age 65 without any additional contributions. Add `computeCoastFireAge(currentAge: number, currentInvested: number, annualExpenses: number, targetAge?: number, realReturn?: number)` to `src/lib/financial-state.ts`. The function should: (1) calculate the FIRE number (annualExpenses / 0.04), (2) find the age at which currentInvested × (1+realReturn)^(targetAge - age) >= fireNumber, (3) return the coast age or null if already past it. Add insight type `"coast-fire"` to insights.ts with messages: if already coasting → "You've hit Coast FIRE! Even if you stopped saving today, your investments would grow to cover retirement by age 65"; if coast age is in the future → "If you keep saving until age X, you could stop contributing and still retire at 65 — that's Y years away". Include a brief explanation of what Coast FIRE means and why it's motivating for young savers. Add `currentAge` field to `FinancialData` (user enters their age in a new input). Unit tests for the calculation at various ages/balances. E2E test. Files: `src/lib/financial-state.ts`, `src/lib/insights.ts`, `src/app/page.tsx`, `src/components/SnapshotDashboard.tsx`. [@fullstack]

- [ ] Task 138: Add net worth milestones and age-based percentile insight — Two new insights: (1) **Net worth milestones**: track and celebrate crossing $0 (for people with student debt — "You've crossed from negative to positive net worth!"), $10k, $50k, $100k, $250k, $500k, $1M with milestone-specific messages explaining what each level unlocks (e.g. "$100k is the hardest milestone — compound growth accelerates from here", "$500k — your money earns more than most people's salaries"). (2) **Age-based percentile**: using Federal Reserve SCF data medians by age group (Under 35: $39k, 35-44: $135k, 45-54: $247k, 55-64: $364k, 65-74: $410k, 75+: $335k), show where the user stands vs peers: "Your net worth of $X puts you above/below the median for your age group ($Y)". Use the `currentAge` field added in Task 136. Add insight types `"net-worth-milestone"` and `"net-worth-percentile"`. Tone should be encouraging regardless of where they stand. Unit tests for milestone detection and percentile lookup. E2E test. Files: `src/lib/insights.ts`, `src/lib/financial-state.ts`. [@fullstack]

- [ ] Task 139: [MILESTONE] New insights E2E regression — Full Playwright regression test verifying all new insight types render correctly: (1) debt-to-income ratio shows correct tier and educational text, (2) housing cost ratio flags 30%+ correctly, (3) Coast FIRE age calculates and displays with explanation, (4) net worth milestones trigger at correct thresholds, (5) age-based percentile shows correct comparison. Test with multiple scenarios (young adult with student debt, mid-career homeowner, high earner). Capture screenshots. Verify no accessibility contrast ratio regressions (text must meet WCAG AA 4.5:1 for normal text). [@qa]
