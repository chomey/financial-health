# Tasks

<!-- Older tasks archived to TASKS-ARCHIVE.md -->


- [x] Task 119: Income replacement ratio metric — Add a new metric card: "Income Replacement" showing what percentage of current income the user's investment portfolio could sustain indefinitely using the 4% rule. Formula: (total invested assets × 0.04 / 12) / monthly income × 100. Show as a percentage with a gauge or progress ring visualization. Tiers: < 25% "Early stage", 25-50% "Building momentum", 50-75% "Strong position", 75-100% "Nearly independent", 100%+ "Financially independent." Add to the insights panel with an encouraging message. Unit tests for the ratio calculation and tier assignment. Files: `src/components/SnapshotDashboard.tsx` (new metric card), `src/lib/financial-state.ts` (computation), `src/lib/insights.ts` (insight message). [@fullstack]

- [x] Task 120: [MILESTONE] E2E test for new financial intelligence features — Comprehensive E2E test covering all tasks 110-119: (1) Inflation toggle changes projection values downward. (2) Age input triggers personalized benchmark comparisons. (3) Employer match appears in asset entries and increases projections. (4) Sample profiles populate all fields correctly. (5) Print layout renders all metrics without interactive elements. (6) Mobile wizard completes and produces valid URL state. (7) Debt payoff strategies show correct avalanche vs snowball comparison. (8) FIRE milestone appears on projection chart at correct value. (9) Tax optimization suggestions appear for suboptimal account mixes. (10) Income replacement ratio displays correct percentage and tier. Capture screenshots for each feature. [@qa]

- [x] Task 121: Auto-display investment returns in Income section — Show auto-computed investment returns as "auto" line items in the Income section UI, mirroring how auto-computed taxes appear in the Expenses section. For each asset with a non-zero ROI, display a read-only row showing "{category} returns" with the monthly amount and an "auto" badge. Data already exists via `computeMonthlyInvestmentReturns()` in `src/lib/financial-state.ts`. Add an "Auto-computed" subsection below manually entered income sources (same pattern as ExpenseEntry's auto-computed tax rows). Include the investment return rows in the Monthly Total. Unit tests for the rendering logic and E2E test verifying auto rows appear when assets have ROI. Files: `src/components/IncomeEntry.tsx`, `src/app/page.tsx` (pass investment returns prop). [@fullstack]

- [ ] Task 122: Fix projection chart label clipping — Y-axis currency labels (e.g. "CA$105.0M") and milestone annotation labels ("Consumer Debt Free", "Mortgage Free") are clipped at the chart edges. (1) Increase the left margin of the Recharts `<ComposedChart>` to accommodate wide currency labels with CA$/US$ prefixes. (2) Move milestone annotation labels so they don't overlap the chart top edge — position them slightly below the top with a background pill/badge so they remain readable over grid lines. (3) Use `compact` currency formatting on the Y-axis (e.g. "CA$105M" not "CA$105.0M") to reduce label width. Unit test for Y-axis formatting, E2E screenshot verifying no clipping. Files: `src/components/ProjectionChart.tsx`. [@frontend]

- [ ] Task 123: Add mortgage burndown line to projection chart — Show a declining mortgage balance line on the Financial Projection chart alongside the existing Net Worth, Assets, and Debts lines. For each property with a mortgage, compute the amortization schedule (using the property's mortgage rate and remaining balance) and plot the declining balance over time. The line should use a distinct style (e.g. dashed orange) and appear in the legend as "Mortgage". When the mortgage reaches $0, it should coincide with the existing "Mortgage Free" milestone marker. Include the mortgage line in the summary table's debt row breakdown. Add the amortization computation to `src/lib/projections.ts` and the chart line to `src/components/ProjectionChart.tsx`. Unit tests for amortization schedule computation. [@fullstack]
