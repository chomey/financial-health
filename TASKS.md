# Tasks

<!-- Older tasks archived to TASKS-ARCHIVE.md -->


- [x] Task 99: Unified chart — always 50 years, X-axis in years, add 40/50yr columns — The merged projection/burndown chart should use a consistent X-axis in years for both modes, always project 50 years, and remove the 10/20/30 year selector buttons. Changes: (1) **Remove the timeline selector buttons** (10/20/30): delete `TIMELINE_OPTIONS`, the `years` state, and the pill buttons in `ProjectionChart.tsx`. Always project 50 years. (2) **X-axis in years for both modes**: "Keep Earning" already uses years. "Income Stops" (burndown) currently uses months — convert to years (divide month by 12). Both modes share the same X-axis range (0–50 years) so switching modes doesn't jump the axis. (3) **Update `TABLE_MILESTONES`** from `[10, 20, 30]` to `[10, 20, 30, 40, 50]` so the summary table and per-asset projection table show 40-year and 50-year columns. (4) **Update `projectFinances`** in `projections.ts`: change the default/max projection from 30 years to 50 years. (5) **Update `simulateRunwayTimeSeries`** cap from 600 months to 600 months (50 years) — already matches, just verify. (6) For the burndown mode, if savings run out before 50 years, the line stays at $0 for the remainder — this visually shows how early money runs out relative to the full timeline. Files: `src/components/ProjectionChart.tsx`, `src/lib/projections.ts`. [@fullstack]

- [x] Task 100: Include investment return taxes in Estimated Tax with correct CA/US rules — Investment returns from taxable accounts generate tax obligations that should be reflected in the Estimated Tax card and explainer. The rules differ by country and account type. **Canada rules**: (A) Interest income (savings, GIC, HISA): 100% included in taxable income annually at marginal rate, even if not withdrawn. (B) Canadian dividends from public corps: eligible for dividend tax credit — gross up by 38%, then apply ~15% federal credit + provincial credit. For simplicity, model as taxed at ~25-33% effective (lower than marginal). (C) Capital gains: only taxed on realization — 50% inclusion rate on first $250k, 66.67% above. Not annual. (D) Tax-sheltered (TFSA, FHSA, RESP): no tax. (E) Tax-deferred (RRSP, LIRA): no annual tax, full withdrawal taxed as income. **US rules**: (A) Interest income (savings, HYSA, CDs, money market): taxed annually as ordinary income at marginal federal rate + state rate. (B) Qualified dividends: taxed at long-term capital gains rates (0%/15%/20%), not ordinary income rates. (C) Capital gains: short-term (<1yr) taxed as ordinary income; long-term (>=1yr) at preferential rates 0%/15%/20%. Only on realization. (D) Tax-sheltered (Roth IRA, Roth 401k, HSA): no tax. (E) Tax-deferred (401k, IRA, 529): no annual tax, full withdrawal taxed as income. **Implementation**: (1) In `computeTotals` in `financial-state.ts`, for each non-tax-sheltered, non-tax-deferred asset with `roiTaxTreatment === "income"`, compute annual interest as `balance * (roi/100)` and add to taxable income using `computeTax` with type `"employment"` (since interest is taxed as ordinary income in both countries). (2) For `roiTaxTreatment === "capital-gains"` on taxable accounts, do NOT add to annual tax — already modeled in withdrawal/runway. (3) In `buildTaxExplainerDetails`, include an `investmentIncomeTax` section with per-account breakdown. (4) In `TaxExplainerContent`, add a section below bracket tables explaining: which accounts generate annual tax, the country-specific rules, and a note: "Interest income is taxed annually. Capital gains and tax-deferred withdrawals are taxed only when realized." (5) Add unit tests verifying: CA interest taxed at marginal rate, US interest taxed at ordinary income rate, capital gains accounts excluded from annual tax, tax-free accounts excluded. Files: `src/lib/financial-state.ts`, `src/components/DataFlowArrows.tsx` (TaxExplainerContent), `tests/unit/financial-state.test.ts`. [@fullstack]

- [x] Task 101: [MILESTONE] E2E test for unified chart and enhancements — Comprehensive E2E test covering: (1) Withdrawal Tax Impact card shows details auto-expanded (no "Show details" toggle), withdrawal order labeled "Suggested" with disclaimer text visible. (2) Clicking Estimated Tax card with $0 income still opens explainer showing jurisdiction bracket info. (3) Runway burndown chart renders on main page below projections (full-width, not in modal). (4) Clicking Estimated Tax card with income opens bracket bar, federal/provincial breakdown, effective vs marginal rates. (5) Asset ROI tax treatment toggle visible on savings/GIC accounts, hidden on TFSA/Roth, defaults correct. (6) Source summary cards show all items with scrollable list and frozen total. (7) All modals close correctly on Escape/click-outside. Capture screenshots. [@qa]

- [x] Task 102: Redesign tax bracket visualization with tiered fill bars — The current "Income by Tax Bracket" bar in the Estimated Tax explainer is a flat stacked horizontal bar that's hard to read. Replace it with a **tiered bucket/waterfall visualization** — the common "water filling up stacked containers" metaphor for progressive taxation. Each bracket is a horizontal bar at full width representing the bracket range, filled proportionally to show how much income falls in that bracket. Stack them vertically (lowest bracket at bottom, highest at top). Each bar shows: bracket range label on left, fill percentage with color gradient (lighter green → deeper teal for higher rates), rate label on the filled portion, and dollar amount on right. Unfilled brackets (above the user's income) show as empty/gray outlines. This replaces both the stacked bar AND the bracket table — the visualization IS the table. Keep the federal and provincial sections separate, each with their own tiered bars. Below each section, show the subtotal. Files: `src/components/DataFlowArrows.tsx` (TaxExplainerContent — replace bracket bar and table with tiered fill bars). [@frontend]

- [x] Task 103: Fix currency formatting in explainer modals — use full numbers with currency code — The explainer modal totals use `fmtShort` which produces ugly abbreviated values like "+$12477k" that are confusing and lose precision. Also, when users have mixed-currency assets (e.g., USD accounts in a CAD home currency), the totals don't indicate which values are converted. Fix: (1) In `SourceSummaryCard` totals (the oval-annotated number), use `formatFullCurrency(value, homeCurrency)` instead of `fmtShort` — show full formatted numbers like "CA$12,476,711" or "$450,000 USD". (2) For individual item rows in SourceSummaryCard, show full formatted amounts with currency code when the item's currency differs from home (e.g., "Fidelity Brokerage: $450,000 USD" vs "TD GIC: CA$3,400,000"). Items in home currency just show the symbol. (3) Pass `homeCurrency` and per-item `currency` through the SourceMetadataItem type and DataFlowContext so the modal knows each item's original currency. (4) Apply the same fix to the Monthly Surplus, Debt-to-Asset, and Financial Runway explainer modals — all totals and breakdowns should use full formatted currency, not `fmtShort`. (5) The metric cards themselves can keep compact formatting (e.g., "CA$12.5M") since space is limited, but the explainer modals have room for full numbers. Files: `src/components/DataFlowArrows.tsx` (SourceSummaryCard, all explainer content), `src/app/page.tsx` (pass currency metadata), `src/lib/financial-state.ts` (update SourceMetadataItem if needed). [@frontend]

- [x] Task 104: Replace Net Worth waterfall chart with donut/pie chart — The `NetWorthWaterfallChart` is hard to read and doesn't effectively communicate the composition of net worth. Replace it with a **donut chart** (recharts PieChart with inner radius) that shows the breakdown of where net worth is held. Segments: each asset category (Savings, TFSA, RRSP, Brokerage, etc.), stocks, property equity — each with distinct colors matching the `AssetAllocationChart` palette. Debts shown as a separate labeled segment or as a "hole" annotation in the center (center text: "Net Worth: $X"). On hover/tap, show a tooltip with the category name, amount, and percentage. If there's property equity, show it as a distinct segment with a subtle pattern/hash to distinguish illiquid from liquid assets. Below the chart, show a compact legend. Keep `ZoomableCard` wrapper. Files: `src/components/NetWorthWaterfallChart.tsx` (rewrite as donut chart or create new `NetWorthDonutChart.tsx`), `src/app/page.tsx` (swap component). [@frontend]

- [ ] Task 105: Include investment interest income in Cash Flow Sankey — The Cash Flow Sankey diagram currently only shows employment/other income flowing to expenses, tax, contributions, and surplus. It's missing investment interest income (from savings, GIC, HISA with `roiTaxTreatment === "income"`), which is real annual cash flow. Add investment returns as income sources: (1) In `CashFlowInput`, add an `investmentReturns: { label: string; monthlyAmount: number }[]` field. (2) In `buildSankeyData`, create income nodes for each investment return source (e.g., "TD GIC interest", "Savings interest") flowing into the same distribution as employment income. (3) In `page.tsx`, compute monthly investment returns using `computeMonthlyInvestmentReturns` and pass them to the Sankey. (4) Use a distinct color (e.g., teal/cyan) for investment income nodes to visually distinguish from employment income. (5) Only include returns from `roiTaxTreatment === "income"` accounts — capital gains aren't realized cash flow. Files: `src/lib/sankey-data.ts` (CashFlowInput, buildSankeyData), `src/components/CashFlowSankey.tsx`, `src/app/page.tsx` (pass investment returns). [@fullstack]

- [ ] Task 106: Enhance Fast Forward what-if scenarios — The current what-ifs (pay off debt, adjust contributions, income change, windfall) are basic. Add more impactful scenarios: (1) **"What if I retired today?"** — sets income to $0 and shows how long savings last (links to burndown chart). Toggle that zeroes all income and shows the runway impact. (2) **"What if I maxed my tax-sheltered accounts?"** — auto-calculates the contribution room for TFSA ($7,000/yr CA), RRSP (18% of income, max ~$31,560), 401k ($23,500 US), IRA ($7,000 US) and shows the 10/20/30yr impact of maxing them vs current contributions. (3) **"What if I downsized housing?"** — slider to reduce property value/mortgage by a percentage, converting equity to liquid assets. (4) **"What if ROI changed?"** — global ROI adjustment slider (e.g., -3% to +3%) to model market conditions. (5) **Scenario presets**: quick-pick buttons like "Conservative" (lower ROI, higher expenses), "Aggressive saver" (max contributions, lower expenses), "Early retirement" (no income at age X). Each scenario shows a before/after comparison card with key metrics (net worth at 10/20yr, runway, monthly surplus). Files: `src/lib/scenario.ts` (new scenario types), `src/components/FastForwardPanel.tsx` (new UI sections), `src/app/page.tsx` (pass additional data). [@fullstack]

- [ ] Task 107: Validate all formulas and fix contextual inconsistencies — Audit every calculated metric on the page to ensure formulas are correct and contextually sensible. Specific checks: (1) **Net Worth**: verify it correctly sums all assets (savings + stocks + property equity) minus all debts, with proper FX conversion. Cross-check the explainer breakdown adds up to the card value. (2) **Monthly Surplus**: verify formula is `afterTaxIncome + investmentReturns - expenses - contributions - mortgage`. Ensure investment returns use the same ROI values shown in asset entries. Ensure tax is computed on the correct income base (employment + interest income). (3) **Estimated Tax**: verify federal + provincial = total. Verify bracket math: each bracket's tax = (amount in bracket × rate), and sum of bracket taxes = subtotal. Verify effective rate = totalTax / totalTaxableIncome. Verify investment interest is included in taxable income for income-type accounts only. (4) **Financial Runway**: verify runway = liquid assets / monthly obligations. Verify "with growth" accounts for compounding. Verify "after tax" correctly applies withdrawal tax rates per account type. (5) **Debt-to-Asset Ratio**: verify = totalDebts / totalAssets, and "without mortgage" variant excludes property mortgage from debts and property value from assets. (6) **Projection chart**: verify surplus accumulation matches the monthly surplus card. Verify debt paydown timelines match amortization math. (7) **Cash flow Sankey**: verify all flows sum correctly (income in = expenses + tax + contributions + surplus out). Write unit tests for any formulas that lack coverage. Fix any bugs found. Files: `src/lib/financial-state.ts`, `tests/unit/formula-validation.test.ts` (new comprehensive test file). [@fullstack]

- [ ] Task 108: [MILESTONE] E2E test for UI polish and formula validation — Comprehensive E2E test covering: (1) Tax bracket tiered fill bars render correctly with income filling proportional amounts. (2) Explainer modals show full formatted currency (no "k" abbreviation), with correct currency codes for mixed-currency portfolios. (3) Net worth donut chart renders with correct segments and hover tooltips. (4) Cash flow Sankey includes investment interest income nodes. (5) Fast Forward panel shows new scenario options (retire today, max tax-sheltered, ROI adjustment). (6) All metric card values match their explainer breakdowns (Net Worth = sum of parts, Tax = federal + provincial, etc.). Capture screenshots. [@qa]
