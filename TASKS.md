# Tasks

<!-- Completed tasks archived to TASKS-ARCHIVE.md. Last completed: Task 124. -->
<!-- Ralph picks up the first unchecked task and works on exactly one per iteration. -->

- [ ] Task 125: [OPUS] Visual theme overhaul — soft cyberpunk palette — Replace the current green/stone Tailwind theme with a soft cyberpunk aesthetic: dark-ish cool background (slate-900/950), muted neon accents (cyan, violet, rose) instead of bright greens, semi-transparent glass-effect cards with subtle borders. The vibe is "easy on the eyes at night" — no blindingly bright colors, no harsh contrasts. Update `globals.css` `:root` and `@theme inline` color tokens. Update `--background` and `--foreground` for the dark base. Add CSS custom properties for accent colors (e.g. `--accent-positive`, `--accent-negative`, `--accent-info`). This task ONLY changes the theme tokens and globals.css — component updates are in subsequent tasks. Unit test that the CSS file contains the new token names. [@frontend]

- [ ] Task 126: Update metric cards and dashboard for new theme — Apply the soft cyberpunk palette to `SnapshotDashboard.tsx` metric cards: dark card backgrounds with glass effect (bg-white/5 backdrop-blur border-white/10), muted cyan for positive values instead of bright green, soft rose for negative, stone-300/400 for labels. Update glow-pulse and warning-pulse keyframes in globals.css to use the new accent colors. Update `DataFlowArrows.tsx` explainer modal: dark background, softer text colors, and fix the tax bracket bars — use dark-on-light text (never white text on light green). Each bracket bar should use a muted gradient that's distinct but not garish. E2E screenshot test verifying readability. Files: `src/components/SnapshotDashboard.tsx`, `src/components/DataFlowArrows.tsx`, `src/app/globals.css`. [@frontend]

- [ ] Task 127: Update entry panels and input sections for new theme — Apply the soft cyberpunk palette to all entry components: `AssetEntry.tsx`, `DebtEntry.tsx`, `IncomeEntry.tsx`, `ExpenseEntry.tsx`, `PropertyEntry.tsx`, `GoalEntry.tsx`. Dark card surfaces, muted text, subtle borders. Input fields should have dark backgrounds with light text. Hover/focus states use soft accent glows instead of bright green highlights. "Add" buttons use muted accent colors. Section totals use the new positive/negative accent colors. Auto-computed rows (tax, investment returns) should have a distinct but subtle badge style. E2E screenshot test. [@frontend]

- [ ] Task 128: Update charts and projection panel for new theme — Apply soft cyberpunk palette to all chart components: `NetWorthDonutChart.tsx`, `ProjectionChart.tsx`, `ExpenseBreakdownChart.tsx`, `WaterfallChart.tsx`. Chart backgrounds transparent/dark, grid lines very subtle (white/5), axis labels in muted light text. Update Recharts color arrays to use muted neon tones (cyan, violet, amber, rose) instead of the current green palette. Projection chart lines should be soft glowing colors. Summary tables use dark rows. Legend text in light muted colors. E2E screenshot test. [@frontend]

- [ ] Task 129: Update page layout, header, and remaining UI for new theme — Apply soft cyberpunk palette to `page.tsx` layout: dark page background, header with subtle gradient or glass effect, country toggle buttons with accent borders, insights panel with dark cards. Update `FastForwardPanel.tsx` sliders and controls. Update print styles in globals.css to override back to white background for printing. Final pass on any remaining hardcoded colors across all components (grep for `bg-white`, `text-stone-`, `border-stone-` etc and update). E2E screenshot of full page. [@frontend]

- [ ] Task 130: [OPUS] Show company/fund names for stock tickers — Create `src/lib/ticker-names.ts` with a static `Record<string, string>` map of ticker symbols to full company/fund names. Include ALL S&P 500 companies, plus popular ETFs (VOO, VTI, VGT, VGK, VFIAX, QQQ, SPY, IWM, etc.) and popular Canadian tickers (XIU, XIC, VFV, etc.) — at minimum 550+ entries. Display the full name as a subtle secondary line below the ticker in both the Stocks & Equity detail view (`AssetEntry.tsx` stock rows) and the explainer modal source cards (`DataFlowArrows.tsx`). For tickers NOT in the static map, make a client-side web search/API call to resolve the name (e.g. a free finance API or search endpoint), then cache the result in a React ref or module-level Map so it's only fetched once per session. Show a brief loading state while fetching. If the lookup fails, show nothing (graceful fallback). Export a `getTickerName(ticker: string): string | undefined` sync function for the static map and a `resolveTickerName(ticker: string): Promise<string | undefined>` async function for the fallback. Unit tests for static lookup and cache behavior. E2E test that names appear for known tickers. Files: `src/lib/ticker-names.ts` (new), `src/components/AssetEntry.tsx`, `src/components/DataFlowArrows.tsx`. [@fullstack]

- [ ] Task 131: [MILESTONE] Visual theme E2E regression — Full Playwright regression test verifying the soft cyberpunk theme renders correctly across all major views: (1) main dashboard with metric cards, (2) entry panels with inputs, (3) explainer modal with tax brackets readable, (4) projection chart with new colors, (5) mobile responsive view, (6) ticker names visible on stock entries. Capture before/after screenshots. Verify no accessibility contrast ratio regressions (text must meet WCAG AA 4.5:1 for normal text). [@qa]
