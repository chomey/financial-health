# Tasks

<!-- Older tasks archived to TASKS-ARCHIVE.md -->

<!-- Completed tasks archived to TASKS-ARCHIVE.md. Last completed: Task 124. -->
<!-- Ralph picks up the first unchecked task and works on exactly one per iteration. -->

- [x] Task 148: Make insights context-aware of user's actual accounts — Insights currently hardcode generic account names like "TFSA, RRSP, 401k" regardless of what the user actually has. Fix: (1) Add `assetCategories: string[]` and `debtCategories: string[]` to `FinancialData` (populated from state in `toFinancialData`). (2) Tax-rate-high insight (line ~274): instead of "tax-advantaged accounts (TFSA, RRSP, 401k)", check what tax-advantaged accounts the user already has and what they're missing. If user has a 401k, say "You're already using your 401k — maximizing contributions and adding a Roth IRA could shelter more." If user has no tax-advantaged accounts, suggest country-appropriate ones. (3) Tax optimization insights: reference the user's actual account names, not generic ones. If the user already has the suggested account type, adjust the message (e.g., "increase your 401k contributions" not "shift to a 401k"). (4) Withdrawal tax insights: use actual account names from the user's data. (5) RRSP/401k contribution insight: skip if user already has no taxable accounts to optimize. (6) General rule: never suggest an account type the user is already maxing out, never suggest Canadian accounts to US users or vice versa. Unit tests with various account mixes (CA-only, US-only, mixed, no tax-advantaged, all tax-advantaged). [@fullstack]

- [x] Task 149: Financial roadmap step definitions and inference engine — Create `src/lib/flowchart-steps.ts` with step definitions for US (r/personalfinance "How to handle $") and CA (r/PersonalFinanceCanada "Money Steps"). **US steps**: (1) Budget & essentials, (2) $1k starter emergency fund, (3) Employer 401(k) match, (4) Pay high-interest debt >8%, (5) Full 3–6 month emergency fund, (6) Max HSA, (7) Max IRA/Roth IRA, (8) Max 401(k), (9) Pay moderate debt 4–8%, (10) Taxable investing & goals. **CA steps**: (1) Budget & essentials, (2) $1k starter emergency fund, (3) Employer RRSP/pension match, (4) Pay high-interest debt >8%, (5) Full 3–6 month emergency fund, (6) TFSA, (7) RRSP, (8) Pay moderate debt 4–8%, (9) RESP/FHSA, (10) Taxable investing. Each step has: id, title, description, completionHint, status (complete/in-progress/upcoming), progress %, detail text. Inference logic: check liquid savings vs expenses for emergency fund months, debt interest rates for high/moderate debt, account existence and balances for investment steps. Steps that can't be inferred (employer match, HSA eligibility, maxing contributions) are marked `userAcknowledgeable: true` with `acknowledgeLabel` and optionally `skippable: true` with `skipLabel`. Export `getFlowchartSteps(state)`, `getCurrentStepIndex(steps)`, `applyUserOverrides(steps, acknowledged, skipped)`. Add URL persistence helpers in `url-state.ts`: `getFlowchartAcksFromURL()`, `getFlowchartSkipsFromURL()`, `updateFlowchartOverridesURL()` using `fca=` and `fcs=` params. Unit tests for step inference with various financial states (no data, partial, fully funded, US vs CA, high debt, no debt, employer match scenarios). [@fullstack]

- [x] Task 150: Financial roadmap visual component — Create `src/components/FinancialFlowchart.tsx` that renders the flowchart steps from Task 149 as an interactive vertical flowchart. Features: (1) Each step is a clickable card with step number circle (green checkmark when complete, amber pulse when in-progress, grey when upcoming). (2) Vertical connector lines between steps (green for completed, grey for upcoming). (3) Expandable detail: click to show full description and user checkboxes. (4) For `userAcknowledgeable` steps, show checkbox ("I'm capturing my full employer match"). For `skippable` steps, show skip checkbox ("No employer match available"). (5) Undo button for acknowledged/skipped steps. (6) Progress bar at top showing X/N steps complete. (7) Auto-expand the current (first non-complete) step. (8) Progress bar with gradient. Persist acknowledge/skip state via URL helpers from Task 149. Wire into `page.tsx`: import component, add "Roadmap" nav link, place in dashboard panel (right column) wrapped in ZoomableCard. Credit r/personalfinance and r/PersonalFinanceCanada communities with links to original wiki pages. Add disclaimer "general guidance, not financial advice." Unit test for rendering. E2E test: verify steps update when data changes, acknowledge/skip persist in URL. [@frontend]

- [x] Task 151: [MILESTONE] Financial roadmap E2E regression — Full Playwright regression for the roadmap feature: (1) Load with default CA data — verify 10 CA steps render, budget step is complete (has income/expenses), emergency fund shows progress, TFSA/RRSP detected. (2) Switch to US — verify steps swap to US variant (401k, HSA, IRA instead of TFSA, RRSP, RESP). (3) Acknowledge employer match — verify step moves to complete, URL contains `fca=` param, reload preserves state. (4) Skip HSA — verify step shows N/A badge, URL contains `fcs=` param. (5) Undo acknowledgement — verify step reverts. (6) Add high-interest debt — verify "Pay High-Interest Debt" step becomes in-progress with dollar amount. (7) Add savings to hit 3-month emergency fund — verify step completes. (8) Verify progress bar updates with each change. Capture screenshots of US and CA roadmaps. [@qa]
