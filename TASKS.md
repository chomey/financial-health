# Tasks

<!-- Older tasks archived to TASKS-ARCHIVE.md -->


- [x] Task 115: Mobile guided wizard entry mode — Add a "Quick entry" wizard mode for mobile viewports (< 768px). When triggered, show a full-screen step-by-step flow: Step 1: Monthly income (with common presets). Step 2: Monthly expenses (with category suggestions). Step 3: Assets (grouped by type). Step 4: Debts. Each step has large touch targets, a progress indicator, and swipe/button navigation. On completion, transition to the normal dashboard view. Store a "wizard completed" flag so returning users go straight to the dashboard. The wizard should pre-fill the same URL state as manual entry. T2 test at mobile viewport verifying wizard flow. Files: `src/components/MobileWizard.tsx` (new), `src/app/page.tsx` (conditional rendering). [@frontend]

- [x] Task 116: Debt payoff strategy comparison — Add a "Payoff strategies" section to the insights panel (or as an expandable card) when the user has 2+ debts. Calculate and compare: (1) **Avalanche** (highest interest rate first) — show total interest paid and payoff date. (2) **Snowball** (smallest balance first) — show total interest paid and payoff date. (3) **Current** (user's current payment allocation). Display the interest savings between strategies: "Switching to avalanche saves $X in interest and pays off debts Y months sooner." Use the existing `debt-payoff.ts` engine or extend it. Add unit tests for both strategies with a multi-debt scenario. Files: `src/lib/debt-payoff.ts` (extend with strategy comparison), `src/components/InsightsPanel.tsx` or new `DebtStrategyCard.tsx`, `src/lib/insights.ts`. [@backend]

- [x] Task 117: FIRE number milestone — Calculate the user's Financial Independence number: annual expenses / safe withdrawal rate (default 4%). Add it as a milestone on the projection chart ("FIRE number: $X") and as a metric or insight: "You need $X to be financially independent. At your current savings rate, you'll reach it in Y years." If the user is already past their FIRE number, celebrate it. Add a configurable withdrawal rate (3-5%) in the Fast Forward panel. Unit tests for FIRE calculation with various expense/withdrawal rate combos. Files: `src/lib/projections.ts` (add FIRE milestone), `src/components/ProjectionChart.tsx` (render milestone), `src/lib/insights.ts` (new FIRE insight), `src/components/FastForwardPanel.tsx` (withdrawal rate slider). [@fullstack]

- [x] Task 118: Tax optimization suggestions — Add smart tax insights that analyze the user's account mix and suggest optimizations: (1) "You have $X in a taxable brokerage earning interest taxed at your marginal rate. Moving contributions to your TFSA/Roth IRA would save ~$Y/year in taxes." (2) "Based on your $X income, you could contribute up to $Y more to your RRSP/401k and reduce your tax bill by ~$Z." (3) "Your TFSA/Roth IRA has room — contributing here instead of your savings account shelters future growth from tax." Use existing tax engine bracket data and withdrawal-tax treatment info. Only show suggestions when there's a meaningful optimization (> $100/year savings). Unit tests for each suggestion type with various account mixes. Files: `src/lib/insights.ts` (new tax optimization insights), `src/lib/tax-engine.ts` (helper for marginal rate lookups), `src/lib/withdrawal-tax.ts`. [@backend]

- [ ] Task 119: Income replacement ratio metric — Add a new metric card: "Income Replacement" showing what percentage of current income the user's investment portfolio could sustain indefinitely using the 4% rule. Formula: (total invested assets × 0.04 / 12) / monthly income × 100. Show as a percentage with a gauge or progress ring visualization. Tiers: < 25% "Early stage", 25-50% "Building momentum", 50-75% "Strong position", 75-100% "Nearly independent", 100%+ "Financially independent." Add to the insights panel with an encouraging message. Unit tests for the ratio calculation and tier assignment. Files: `src/components/SnapshotDashboard.tsx` (new metric card), `src/lib/financial-state.ts` (computation), `src/lib/insights.ts` (insight message). [@fullstack]

- [ ] Task 120: [MILESTONE] E2E test for new financial intelligence features — Comprehensive E2E test covering all tasks 110-119: (1) Inflation toggle changes projection values downward. (2) Age input triggers personalized benchmark comparisons. (3) Employer match appears in asset entries and increases projections. (4) Sample profiles populate all fields correctly. (5) Print layout renders all metrics without interactive elements. (6) Mobile wizard completes and produces valid URL state. (7) Debt payoff strategies show correct avalanche vs snowball comparison. (8) FIRE milestone appears on projection chart at correct value. (9) Tax optimization suggestions appear for suboptimal account mixes. (10) Income replacement ratio displays correct percentage and tier. Capture screenshots for each feature. [@qa]
